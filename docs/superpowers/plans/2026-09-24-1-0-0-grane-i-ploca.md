# 1.0.0 — grane · ploča · izlaz: plan implementacije

**Status:** 📋 AKTIVAN — napisan 2026-09-24 iz odobrenog speca [`../../plan/ARHITEKTURA_1_0.md`](../../plan/ARHITEKTURA_1_0.md);
izvodi se agentima po [`../../workflow/AGENTI.md`](../../workflow/AGENTI.md). Nastavlja numeraciju cigli plana M2
([`2026-09-18-m2-desktop.md`](./2026-09-18-m2-desktop.md), zadnja cigla T43): **T44–T63**. Ovo je **plan**, ne izvor
istine: što je izgrađeno kaže [`../../architecture/ARCHITECTURE.md`](../../architecture/ARCHITECTURE.md), što je
isporučeno [`../../records/CHANGELOG.md`](../../records/CHANGELOG.md), odstupanja od plana
[`../../records/PROGRESS.md`](../../records/PROGRESS.md).

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Sokratis 1.0.0 mjeri **sve lokalne grane** (Leonov rad u radnim stablima je u brojkama, s granom po commitu), čita dnevnik iz svih stabala, otvara **nadzornu ploču projekta** s 8 sekcija i 11 grafova s datumima na osima, zatvara se na X uz upit bez tray-ikone i ne bljeska konzolu `git.exe`.

**Architecture:** `io` bira reference (`Scope::AllBranches` → `git log --branches`) i daje jezgri odvojenu kartu `sha → grana` iz jednog dodatnog procesa; jezgra ostaje čista (S-002) i dobiva `CommitRow.branch`, `Report.branches` i uniju dnevnika; keš po SHA-i se ne dira (S-014). Sučelje dobiva zajedničke temelje grafova (`Chart · Axis · Grid · Legend · Tooltip`) nad d3-jezgrenim modulima bez DOM-a (S-035) i ploču `Projekt` koja zamjenjuje šest prikaznih pogleda (S-034). Desktop gubi `tray.rs`, X emitira `close_requested`, naredba `quit` gasi proces (S-036); `git_command()` je jedino mjesto `Command::new` (kvar 4).

**Tech Stack:** kao M2 (Rust 1.98.1 pinan · Tauri 2.11.5 · Svelte 5.57.0 · Vite 8.3.0 · Tailwind 4.3.3 · TypeScript 5.9.3 · vitest 5.0.1) **+ 4 npm ovisnosti** uz Leonov OK (S-035): `d3-scale@4.0.2` · `d3-shape@3.2.0` · `d3-array@3.2.4` · `d3-time-format@4.1.0` i njihovi `@types/*` kao dev. **Nula novih Rust ovisnosti**; Tauri feature `tray-icon` se **uklanja**.

**Spec:** `docs/plan/ARHITEKTURA_1_0.md` (odobren 2026-09-24) · nalazi: `docs/product/NALAZI_LEON_2026-09-23.md` · odluke: `docs/records/DECISIONS.md` (S-032…S-037) · konvencije: `docs/workflow/RUST.md` · testovi: `docs/workflow/TESTING.md` · agenti: `docs/workflow/AGENTI.md` · Sokrat Study: `C:\Users\leonk\Documents\sokratstudy.dev` + stabla `.f21 .f22 .f25 .f3 .f6` (**samo čitanje**).

## Global Constraints

- `sokratis-core` **ne smije** koristiti `std::fs`, `std::process`, `std::env` ni `std::time` (S-002). Oznaka grane po commitu stiže u jezgru kao `HashMap<String, String>` u `ReportInput`; jezgra nikad ne pita git.
- **`Report` je ugovor** (S-012) i mijenja se **samo namjerno**: T46/T47/T48 mijenjaju snapshot `crates/sokratis-core/tests/snapshots/snapshot__report-sokratstudy-2026-09-17.snap` kroz `INSTA_UPDATE=always cargo test -p sokratis-core --test snapshot`, pregled diffa u `git diff`, rečenica u commitu koje polje i zašto (S-022). Cigla koja promijeni snapshot bez te rečenice = VRATI. Ime datoteke snapshota **ostaje isto** (sučelje ga uvozi po imenu s `?raw`).
- **Paritet s `RAD.xlsx` je test** (`crates/sokratis-core/tests/parity.rs`) i **ne mijenja se**: fixture je log `main`-a, `scope` u njemu je `DefaultBranch`, `commit_branches` prazna, `diaries` = jedan tekst. Ako paritet padne, cigla je kriva, ne fixture.
- **Keš commita (`sokratis-store`, `commit_cache`) se ne mijenja** — nema migracije; oznaka grane NIJE svojstvo SHA-e (S-032) i ne ide u keš.
- Identifikatori u kodu, JSON-u i i18n-ključevima su **engleski** (`snake_case` Rust · `camelCase` TS) (S-008). Nazivi grana su podaci, ne identifikatori — prolaze kakvi jesu.
- **Zadano `branch_scope = "all"`** za sve projekte (S-032, dopuna S-005); profil pregazi; CLI `--scope` pregazi profil.
- **Ovisnosti**: samo T53 smije mijenjati `apps/desktop/package.json` blok `dependencies`/`devDependencies` (točne verzije, bez `^`; `package-lock.json` se commita; `npm ci` nakon toga ne smije mijenjati lock). Nijedna druga cigla ne dodaje ovisnost; tok koji je treba STAJE i javlja orkestratoru. Rust `Cargo.toml` mijenja SAMO T45 (uklanja feature `tray-icon`).
- **Vlasništvo datoteka po toku** (tablica dolje). Tok ne dira tuđe datoteke; ako mora, STAJE i javlja. Iznimke su navedene po cigli („samo redci konstrukcije").
- Svaka Rust cigla: `cargo fmt --check` · `cargo clippy --workspace --all-targets -- -D warnings` · `cargo test --workspace` zeleni prije commita (workspace, ne samo crate — promjena ugovora `Report` dira tri cratea); zaglavlje `//! ZAŠTO RUST OVAKO (cigla M2/N — naziv)` 2–5 redaka u svakoj novoj/ispunjenoj datoteci. Svaka Svelte/TS cigla: `npm run check` (svelte-check · i18n · kontrast · vitest) i `npm run build` u `apps/desktop` zeleni. Zaglavlje u TS/Svelte datotekama je `// ZAŠTO OVAKO (cigla M2/N — naziv)`.
- **Graditelji ne pokreću `npm run tauri dev`** (Ruling R14): pravu ljusku i piksele mjeri orkestrator (CDP, `cdp.mjs` u mapi ledgera) u dimnom testu. Graditelj Rust-desktop cigle dokazuje `cargo build -p sokratis-desktop` + `cargo test --workspace`.
- Bez `unwrap()`/`expect()` izvan `#[cfg(test)]` i `tests/`, osim dva postojeća `expect` u `lib.rs::run()`.
- Commit poruka: `M2/N: <što> -- <zašto u pola rečenice>`; graditelj ne pusha; nikad na `main` (spaja orkestrator). **Sokrat Study se nikad ne mijenja** — mjerenja nad njim su ručna, zapisana u izvještaju cigle. **Tajne i imena ljudi** se traže prije svakog commita (repo je javan, S-023).
- **Leonov OK je već dan** za 4 npm ovisnosti (S-035, 2026-09-24) — T53 ga ne pita ponovno. Sve ostalo što traži OK (tag, licenca, nova ovisnost izvan te četiri, zahvati u Sokrat Studyju, autostart iz razvojne verzije) STAJE.
- **Boje samo tokeni** (`var(--color-*)`), nijedna heksadecimalna vrijednost u grafovima; `npm run check:contrast` ostaje brana. Animacije samo kroz klase iz `styles/motion.css` (S-026) — nov graf koji želi ulaz dodaje klasu tamo, ne inline `animation`.

## Review Focus

Ulazi koje spec podrazumijeva, a koje bez posebnog testa lako promaknu — svaki redak ima svoj test u cigli koja posjeduje kod:

1. **Repo s jednom granom i bez sporednih stabala** (Sokratis sam): `git log --branches` = `main`, `commit_sources` (`--branches --not main`) vraća praznu kartu, vodeće stablo = `root`, `Report.branches` ima točno jedan red → **T49** (`commit_sources_is_empty_when_only_default_branch_exists`), **T50** (`lead_is_root_when_there_is_one_worktree`), **T46** (`single_branch_gives_one_row`).
2. **Sporedno stablo u detached HEAD-u** (`git worktree add --detach`): `worktree list --porcelain` nema redak `branch`, samo `HEAD`; parser ne smije pasti, stablo i dalje ulazi u izbor vodećeg → **T50** (`worktree_heads_parses_detached_worktree`).
3. **Ime grane s `|` ili `/`** (`feat/f6-mcp`, `x|y`): redak `%h|%S` se dijeli na PRVOM `|` (`splitn(2, '|')`), SHA nikad ne sadrži `|` → **T49** (`commit_sources_keeps_pipe_inside_branch_name`).
4. **Raspon bez ijednog commita** (`custom` prozor u budućnosti): `branch_stats` vraća prazan vektor, `bucketDays([])` prazan niz, `layout` bez NaN u koordinatama, sekcija Grane pokazuje prazno stanje → **T46** (`empty_commits_give_no_rows`), **T53** (`bucketDays vraća [] za prazan ulaz`), **T54** (`frame/barsLayout bez NaN za prazan ulaz`).
5. **Dnevnik postoji SAMO u sporednom stablu** (glavno stablo `main` ga nema, npr. nov projekt čiji je dnevnik tek u grani): `diaries` ima jedan tekst, isporuke se vide, `touched.diaries == 1` → **T50** (`diary_only_in_secondary_worktree_still_counts`).
6. **`--scope default` mora dati današnji rezultat**: nad istim repoom s feature-granom `days`/`kinds` u `default` jednaki onome što je 1.0.0-pre.1 računao (bez feature commita) → **T51** (`scope_default_matches_pre_1_0_behaviour`).

---

## Struktura datoteka i vlasništvo po tokovima

| tok | grana | worktree | zadaci | vlasništvo (smije mijenjati SAMO ovo) |
|---|---|---|---|---|
| **DESKTOP-2** | `feat/desktop-2` | `sokratis.desktop2` | T44–T45 | `crates/sokratis-io/src/git.rs` (**samo T44**: `run`, `run_with_stdin`, nov `git_command`, testovi), `apps/desktop/src-tauri/**` (uklj. `icons/tray.png` — brisanje), `Cargo.toml` korijena (**samo** redak `tauri = …`), `apps/desktop/src/lib/api.ts` (samo `quit`/`onCloseRequested`), `apps/desktop/src/lib/shell/ConfirmQuit.svelte` (nov), `apps/desktop/src/App.svelte` (samo ožičenje dijaloga), `apps/desktop/src/lib/i18n/{hr,en}.json` (samo ključevi `quit.*` i `settings.autostart.hint`), `apps/desktop/tests/tauri-api.test.ts` |
| **JEZGRA-2** | `feat/core-branches` | `sokratis.jezgra2` | T46–T48 | `crates/sokratis-core/**` + **samo redci konstrukcije `ReportInput`/`Report`** u `crates/sokratis-io/src/project.rs`, `crates/sokratis-io/src/cache.rs` (testovi), `crates/sokratis-cli/src/table.rs` (testovi), `apps/desktop/src/lib/types.ts` + `apps/desktop/tests/types.test.ts` (zrcalo ugovora, T48) |
| **IO-2** | `feat/io-branches` | `sokratis.io2` | T49–T52 | `crates/sokratis-io/**`, `crates/sokratis-cli/**` |
| **GRAFOVI** | `feat/charts` | `sokratis.grafovi` | T53–T57 | `apps/desktop/src/lib/charts/**`, `apps/desktop/tests/{scale,bucket,layout}.test.ts`, `apps/desktop/package.json` (**samo T53**, blok ovisnosti), `apps/desktop/package-lock.json`, `apps/desktop/src/styles/motion.css` (samo nove `chart-*` klase) |
| **PLOČA** | `feat/ploca` | `sokratis.ploca` | T58–T62 | `apps/desktop/src/**` **osim** `lib/charts/**`, `apps/desktop/tests/**` osim GRAFOVI datoteka |
| **IZDANJE** (orkestrator + graditelj) | `feat/release` | `sokratis.rel` (već postoji, T35 napola) | T35 · T63 · T43 | `Cargo.toml`/`package.json`/lockovi (verzija), `.sokratis/profile.json`, `docs/**` |

**Ovisnosti među tokovima (orkestrator sekvencira spajanja):**

```
main c7c009f…17c7c41 (spec)
 ├─ DESKTOP-2 T44 → T45                     odmah; paralelno s JEZGROM-2 (dijele samo git.rs: T44 dira `run`/`run_with_stdin`, JEZGRA-2 ga ne dira)
 ├─ JEZGRA-2 T46 → T47 → T48                odmah; sekvencijalno u svom stablu
 │    └─ IO-2 T49 → T50 → T51 → T52         tek kad je JEZGRA-2 u main-u (tipovi `BranchScope`, `diaries`, `commit_branches`)
 ├─ GRAFOVI T53 → T54 → T55 → T56 → T57     odmah; bez Rusta; paralelno sa svime (dira samo lib/charts + testove)
 └─ PLOČA T58 → T59 → T60 → T61             tek kad su DESKTOP-2 (App.svelte), JEZGRA-2 (types.ts), IO-2 (`branches` u pravom izvještaju) i GRAFOVI (komponente) u main-u
      └─ T62 dimni test (orkestrator)       nakon spajanja PLOČE
IZDANJE T35 → T63 (završna recenzija, opus) → jedan krug popravaka → T43 (čuvar) → instalater 1.0.0 → STANI (tag traži Leonov OK)
```

Istodobno najviše 4 graditelja (AGENTI.md §5). **Prvi val (sesija 1):** DESKTOP-2 T44, JEZGRA-2 T46, GRAFOVI T53. Nakon svake sesije orkestrator gradi instalater „1.0.0-pre.N" na `main`-u (S-037) i šalje čuvara A (docs).

**Redoslijed sesija (S-037, „vrijednost prvo"):**

| sesija | cigle | Leon vidi (instalater „1.0.0-pre.N") |
|---|---|---|
| 1 | T44 · T45 · T46 · T53 (+ T47 ako stigne) | bez bljeska konzole; X pita i izlazi; animacija pri svakom pokretanju; tray-ikone nema |
| 2 | T47 · T48 · T49 · T50 · T54 | — (jezgra/io; sučelje još ne crta grane) |
| 3 | T51 · T52 · T55 · T56 · T57 · T58 | **njegov rad od 13. 9. u brojkama**; Dnevnik s granom; Isporuke svježe; ploča s postojećim grafovima na temeljima (datumi na osima) |
| 4 | T59 · T60 · T61 · T62 | ploča s 11 grafova, 7 novih objašnjenja |
| 5 | T35 · T63 · popravci · T43 · instalater | 1.0.0 |

```
sokratis/
  Cargo.toml                                         # tauri features: bez "tray-icon" (T45)
  crates/sokratis-core/
    src/model.rs                                     # + BranchScope, BranchStats, CommitRow.branch, Report.scope/branches, ReportInput.scope/commit_branches/diaries/worktrees, Touched.worktrees/diaries (T46, T47)
    src/metrics/branches.rs                          # NOVO: branch_stats (T46)
    src/metrics/kinds.rs                             # commit_rows + karta grana (T46)
    src/metrics/mod.rs                               # + pub use branches::branch_stats (T46)
    src/parse/diary.rs                               # + parse_diaries (unija) (T47)
    src/report.rs                                    # scope, branches, diaries, touched (T46, T47)
    src/profile.rs                                   # + branch_scope: BranchScope (T48)
    tests/branches.rs · tests/diary_union.rs         # NOVO (T46, T47)
    tests/snapshot.rs · tests/parity.rs              # konstrukcija ReportInput (T46, T47) · snapshot regeneriran (T48)
  crates/sokratis-io/
    src/git.rs                                       # git_command (T44) · Scope, commit_sources, worktree_heads, commit_times, last_changes(rev, …) (T49, T50)
    src/cache.rs                                     # cached_log(scope) (T49)
    src/project.rs                                   # scope + karta (T49) · Lead, diaries, docs_in (T50)
    tests/git_cli.rs · tests/cache.rs · tests/project.rs · tests/perf.rs   # (T49, T50)
  crates/sokratis-cli/
    src/main.rs · src/table.rs · tests/cli.rs        # --scope, redak opsega (T51)
  apps/desktop/src-tauri/
    src/lib.rs                                       # bez tray; close_requested; quit (T45)
    src/autostart.rs                                 # NOVO: set_autostart (iz tray.rs) (T45)
    src/tray.rs · icons/tray.png                     # BRISANO (T45)
    src/commands.rs                                  # quit; set_setting → autostart::set_autostart (T45)
  apps/desktop/
    package.json · package-lock.json                 # + d3-scale, d3-shape, d3-array, d3-time-format (+ @types) (T53)
    src/lib/charts/scales.ts · bucket.ts · layout.ts # NOVO (T53, T54, T56, T57)
    src/lib/charts/Chart.svelte · Axis.svelte · Grid.svelte · Legend.svelte · Tooltip.svelte   # NOVO (T54)
    src/lib/charts/Bars.svelte · Line.svelte · Ring.svelte · Sparkline.svelte                 # prepisani na temelje (T55)
    src/lib/charts/Heatmap.svelte · Histogram.svelte # NOVO (T56)
    src/lib/charts/Gantt.svelte · HBars.svelte       # NOVO (T57)
    src/lib/charts/scale.ts                          # BRISANO u T55 (zamjenjuju ga scales.ts + layout.ts)
    src/lib/types.ts                                 # zrcalo: scope, branches, CommitRow.branch, Touched (T48) · View bez šest pogleda (T58)
    src/lib/api.ts                                   # quit, onCloseRequested (T45)
    src/lib/shell/ConfirmQuit.svelte                 # NOVO (T45)
    src/lib/shell/Topbar.svelte · Sidebar.svelte     # bez birača; „‹ Pregled"; 5 stavki (T58)
    src/lib/state.svelte.ts                          # enterProject, leaveProject (T58)
    src/views/Project.svelte                         # NOVO: ploča s 8 sekcija + skok-izbornik (T58)
    src/views/project/*.svelte                       # NOVO: Summary · Tempo · Branches · Kinds · Phases · Deliveries · Indicators · Docs (T58–T60)
    src/views/{Tempo,Kinds,Indicators,Phases,Deliveries,Docs}.svelte   # BRISANO (T58; sadržaj seli u sekcije)
    src/lib/explain/ids.ts · src/lib/i18n/{hr,en}.json                  # 7 novih id-eva + tekstovi (T61)
    tests/bucket.test.ts · tests/layout.test.ts      # NOVO (T53, T54)
    tests/scale.test.ts                              # prilagođen na scales.ts (T55)
    tests/types.test.ts · tests/explain.test.ts · tests/helpers.test.ts   # prošireni (T48, T58, T61)
```

---
## Tok DESKTOP-2 — konzola i izlaz (spec §4)

### Task 44: `git_command()` — jedino mjesto `Command::new("git")`, bez konzolnog prozora (spec §4.2, kvar 4)

**Files:**
- Modify: `crates/sokratis-io/src/git.rs` (`run` ≈ 171–182, `run_with_stdin` ≈ 190–222, zaglavlje, `#[cfg(test)] mod tests` na dnu)

**Interfaces:**
- Consumes: `std::process::Command`, `std::os::windows::process::CommandExt` (samo Windows, std).
- Produces: `fn git_command(repo: &Path) -> Command` (privatna, `git.rs`) — `run` i `run_with_stdin` je zovu; nitko drugi u workspaceu ne smije zvati `Command::new("git")` (T49/T50 dodaju nove `git` pozive KROZ `run`, pa ovo vrijedi i za njih).

- [ ] **Step 1: Napiši padajući test (izvor kao dokaz)**

Na dno `crates/sokratis-io/src/git.rs` (unutar postojećeg `#[cfg(test)] mod tests`, ili nov `mod tests` ako ga nema):

```rust
    /// Zastavica `CREATE_NO_WINDOW` se iz `Command`-a ne može pročitati natrag (std nema getter),
    /// pa test čita IZVOR: `Command::new` smije postojati SAMO unutar `git_command`. Pošteno
    /// ograničenje, zapisano u zaglavlju datoteke.
    #[test]
    fn command_new_lives_only_inside_git_command() {
        let src = include_str!("git.rs");
        let occurrences = src.matches("Command::new(").count();
        assert_eq!(occurrences, 1, "Command::new smije postojati samo u git_command()");
        let helper_start = src.find("fn git_command(").expect("git_command postoji");
        let helper_end = src[helper_start..].find("\n}\n").expect("kraj git_command") + helper_start;
        let helper = &src[helper_start..helper_end];
        assert!(helper.contains("Command::new("), "Command::new je unutar git_command");
        assert!(helper.contains("creation_flags("), "git_command postavlja creation_flags");
        assert!(helper.contains("0x0800_0000"), "CREATE_NO_WINDOW = 0x08000000");
    }
```

- [ ] **Step 2: Pokreni test — mora pasti**

Run: `cargo test -p sokratis-io --lib command_new_lives_only_inside_git_command`
Expected: FAIL — `occurrences` je 2 (`run` i `run_with_stdin`), `git_command` ne postoji.

- [ ] **Step 3: Pomoćnik + oba pozivatelja**

U `git.rs`, iznad `fn spawn_error`:

```rust
/// JEDINO mjesto koje gradi `git` proces (kvar 4, spec 1.0.0 §4.2). Instalirana aplikacija je
/// `windows_subsystem = "windows"` (`main.rs`), pa bi svaki dijete-proces bez zastavice na Windowsu
/// nakratko otvorio konzolni prozor — pri svakom osvježenju, watcheru, promjeni raspona.
/// `CREATE_NO_WINDOW` (0x08000000, `winbase.h`) to gasi; na drugim sustavima ništa se ne mijenja.
/// `#[cfg(windows)]` bira kod PRI KOMPILACIJI, pa `CommandExt` ne postoji na Linuxu ni u `cargo check`
/// za druge mete — nema `if cfg!` s mrtvom granom.
fn git_command(repo: &Path) -> Command {
    let mut cmd = Command::new("git");
    cmd.arg("-C").arg(repo);
    #[cfg(windows)]
    {
        use std::os::windows::process::CommandExt;
        const CREATE_NO_WINDOW: u32 = 0x0800_0000;
        cmd.creation_flags(CREATE_NO_WINDOW);
    }
    cmd
}
```

`run` postaje:

```rust
    fn run(&self, args: &[&str]) -> Result<String, IoError> {
        self.calls.set(self.calls.get() + 1);
        let out = git_command(&self.repo)
            .args(args)
            .output()
            .map_err(spawn_error)?;
        output_to_result(out, &self.repo, args)
    }
```

`run_with_stdin` — prvi redak tijela nakon brojača:

```rust
        let mut child = git_command(&self.repo)
            .args(args)
            .stdin(Stdio::piped())
            .stdout(Stdio::piped())
            .stderr(Stdio::piped())
            .spawn()
            .map_err(spawn_error)?;
```

Zaglavlje datoteke dobiva odlomak:

```rust
//! Cigla M2/44 (konzolni prozor, kvar 4 iz Leonovih nalaza): `git_command` je JEDINO mjesto koje
//! gradi `Command::new("git")` i na Windowsu postavlja `CREATE_NO_WINDOW` kroz `CommandExt`
//! (`#[cfg(windows)]` — kod za drugu platformu se ne kompilira, ne samo ne izvršava). Zastavica se
//! ne može pročitati natrag, pa test `command_new_lives_only_inside_git_command` čita IZVOR.
```

- [ ] **Step 4: Testovi prolaze**

Run: `cargo test -p sokratis-io` · `cargo clippy --workspace --all-targets -- -D warnings` · `cargo fmt --check`
Expected: PASS; `tests/perf.rs` i dalje ≤ 5 procesa (pomoćnik ne dodaje proces).

- [ ] **Step 5: Ručni dokaz (graditelj, bez `tauri dev`)**

Run u `apps/desktop/src-tauri`: `cargo build -p sokratis-desktop` — mora proći (isti `io`). Zapiši u `task-44-report.md`: broj `Command::new` u `git.rs` = 1; `grep -rn "Command::new" crates apps/desktop/src-tauri/src --include=*.rs | grep -v tests` = samo `git_command`.

- [ ] **Step 6: Commit**

```bash
git add crates/sokratis-io/src/git.rs
git commit -m "M2/44: git_command() jedino mjesto Command::new, CREATE_NO_WINDOW na Windowsu -- instalirana GUI aplikacija je bljeskala konzolu git.exe pri svakom osvjezenju (kvar 4, spec 1.0.0 s4.2)"
```

---

### Task 45: X = upit → izlaz, tray uklonjen, autostart otvara prozor (spec §4.1, §3.4, S-036)

**Files:**
- Modify: `Cargo.toml` (korijen, redak `tauri = { version = "2.11.5", features = ["tray-icon", "image-png"] }`)
- Modify: `apps/desktop/src-tauri/src/lib.rs` (cijela `run`)
- Create: `apps/desktop/src-tauri/src/autostart.rs` (seli `set_autostart` iz `tray.rs`)
- Delete: `apps/desktop/src-tauri/src/tray.rs`, `apps/desktop/src-tauri/icons/tray.png`
- Modify: `apps/desktop/src-tauri/src/commands.rs` (`set_setting` → `crate::autostart::set_autostart`; nova naredba `quit`; zaglavlje)
- Modify: `apps/desktop/src/lib/api.ts` (`Api` += `quit`, `onCloseRequested`; `MockApi` i `TauriApi`)
- Create: `apps/desktop/src/lib/shell/ConfirmQuit.svelte`
- Modify: `apps/desktop/src/App.svelte` (ožičenje dijaloga), `apps/desktop/src/lib/i18n/hr.json`, `en.json` (ključevi `quit.*`, `settings.autostart.hint`)
- Test: `apps/desktop/tests/tauri-api.test.ts`

**Interfaces:**
- Consumes: `splash::show_main` (ostaje za `single-instance`), `engine::refresh_all` (ostaje za naredbu `refresh(None)`), `state::text`.
- Produces: Rust naredba `quit() -> Result<(), String>` (zove `app.exit(0)`); Tauri događaj `close_requested` bez tereta; `pub(crate) fn autostart::set_autostart(app: &AppHandle, on: bool) -> Result<(), String>` (isto tijelo kao prije). TS: `Api.quit(): Promise<void>`, `Api.onCloseRequested(cb: () => void): () => void`.

- [ ] **Step 1: Padajući test ugovora TauriApi**

U `apps/desktop/tests/tauri-api.test.ts`, u `describe('TauriApi — ugovor prema commands.rs')`:

```ts
  it('quit zove naredbu quit bez argumenata', async () => {
    invokeMock.mockResolvedValue(undefined);
    const t = new TauriApi();
    await t.quit();
    expect(invokeMock).toHaveBeenCalledWith('quit');
  });

  it('onCloseRequested sluša događaj close_requested i odjavljuje se', async () => {
    const unlisten = vi.fn();
    listenMock.mockResolvedValue(unlisten as unknown as UnlistenFn);
    const t = new TauriApi();
    const cb = vi.fn();
    const off = t.onCloseRequested(cb);
    expect(listenMock).toHaveBeenCalledWith('close_requested', expect.any(Function));
    const handler = listenMock.mock.calls[0][1] as (e: { payload: null }) => void;
    handler({ payload: null });
    expect(cb).toHaveBeenCalledTimes(1);
    off();
    await Promise.resolve();
    expect(unlisten).toHaveBeenCalledTimes(1);
  });
```

Run: `npm run test -- tests/tauri-api.test.ts` (u `apps/desktop`) → FAIL: `t.quit is not a function`.

- [ ] **Step 2: `Api` + `MockApi` + `TauriApi`**

U `api.ts`, u `interface Api` iza `onSignalRaised`:

```ts
  /** X na prozoru (S-036): Rust `prevent_close` + `close_requested` → sučelje pita → `quit()` gasi proces. */
  quit(): Promise<void>;
  onCloseRequested(cb: () => void): () => void;
```

`MockApi` (preglednik/vitest — nema prozora koji bi se zatvorio):

```ts
  async quit(): Promise<void> {
    /* preglednik: nema procesa za ugasiti */
  }

  onCloseRequested(_cb: () => void): () => void {
    return () => {};
  }
```

`TauriApi`:

```ts
  async quit(): Promise<void> {
    await invoke<void>('quit');
  }

  onCloseRequested(cb: () => void): () => void {
    const pending = listen<null>('close_requested', () => cb());
    return deferredUnlisten(pending);
  }
```

- [ ] **Step 3: Rust — `autostart.rs`, `quit`, `lib.rs` bez traya**

`apps/desktop/src-tauri/src/autostart.rs` (novo; tijelo funkcije je DOSLOVNO `set_autostart` iz `tray.rs`):

```rust
//! ZAŠTO RUST OVAKO (cigla M2/45 — autostart bez traya, S-036)
//! `set_autostart` je do sada živio u `tray.rs` jer ga je zvala tray-kvačica; tray je ukinut
//! (X = upit → izlaz), a autostart ostaje postavka u Postavkama. Funkcija seli NEPROMIJENJENA u
//! vlastitu datoteku: plugin pa baza, tim redom — greška plugina se ne upisuje u bazu (stanje u
//! bazi ne smije lagati o OS-u). Jedini pozivatelj je `commands::set_setting`.
use crate::state::{AppState, text};
use tauri::{AppHandle, Manager};
use tauri_plugin_autostart::ManagerExt;

pub(crate) fn set_autostart(app: &AppHandle, on: bool) -> Result<(), String> {
    let result = if on {
        app.autolaunch().enable()
    } else {
        app.autolaunch().disable()
    };
    result.map_err(text)?;
    app.state::<AppState>()
        .store
        .lock()
        .map_err(text)?
        .set_setting("autostart", if on { "on" } else { "off" })
        .map_err(text)
}
```

`commands.rs`: u `set_setting` zamijeni `crate::tray::set_autostart` s `crate::autostart::set_autostart`; dodaj naredbu (iza `refresh`):

```rust
/// Izlaz iz aplikacije (S-036): X na prozoru NE gasi proces sam — `lib.rs` ga presretne
/// (`prevent_close`) i emitira `close_requested`; sučelje pokaže upit i, na „Zatvori", zove OVO.
/// `app.exit(0)` prekida i nit motora usred izračuna: snimke su SQLite transakcije (S-014), pa
/// prekid ne ostavlja pola dana u bazi. Ništa se ne sprema prije izlaza — nema što (registar i
/// postavke se pišu odmah pri promjeni).
#[tauri::command]
pub fn quit(app: tauri::AppHandle) {
    app.exit(0);
}
```

`lib.rs` — nova verzija `run` (cijela; `mod tray` → `mod autostart`, `quit` u handleru, X emitira događaj, `setup` bez `tray::build`):

```rust
mod autostart;
mod cache;
mod commands;
mod engine;
mod splash;
mod state;
mod summary;

use commands::{
    add_project, get_report, get_settings, get_trend, list_projects, quit, refresh, remove_project,
    rename_project, save_visions, set_override, set_setting,
};
use state::AppState;
use std::collections::HashMap;
use std::sync::Mutex;
use tauri::Emitter;
use tauri_plugin_autostart::MacosLauncher;

pub fn run() {
    tauri::Builder::default()
        .plugin(tauri_plugin_single_instance::init(|app, _args, _cwd| {
            splash::show_main(app);
        }))
        .plugin(tauri_plugin_dialog::init())
        .plugin(tauri_plugin_notification::init())
        .plugin(tauri_plugin_autostart::init(
            MacosLauncher::LaunchAgent,
            None,
        ))
        .manage(AppState {
            store: Mutex::new(
                sokratis_store::Store::open(&state::db_path()).expect("baza u %LOCALAPPDATA%"),
            ),
            reports: Mutex::new(HashMap::new()),
            watcher: Mutex::new(None),
            queue: Mutex::new(sokratis_io::RefreshQueue::default()),
            rx: Mutex::new(None),
        })
        .invoke_handler(tauri::generate_handler![
            list_projects,
            add_project,
            rename_project,
            remove_project,
            get_report,
            get_trend,
            set_override,
            save_visions,
            refresh,
            get_settings,
            set_setting,
            quit
        ])
        // X NE zatvara odmah (S-036): `prevent_close` zadrži prozor, `close_requested` ode sučelju,
        // koje pokaže upit („Zatvoriti Sokratis?"); „Zatvori" zove naredbu `quit` → `app.exit(0)`.
        // Traya više nema — svako pokretanje je nov proces, pa i animacija (S-019 ostaje).
        .on_window_event(|window, event| {
            if let tauri::WindowEvent::CloseRequested { api, .. } = event
                && window.label() == "main"
            {
                api.prevent_close();
                if let Err(e) = window.emit("close_requested", ()) {
                    eprintln!("glavni prozor: close_requested nije poslan: {e}");
                }
            }
        })
        .setup(|app| {
            splash::arm(app.handle());
            engine::start(app.handle());
            Ok(())
        })
        .run(tauri::generate_context!())
        .expect("Tauri se nije pokrenuo");
}
```

Zaglavlje `lib.rs` dobiva odlomak `//! ZAŠTO RUST OVAKO (cigla M2/45 — X = upit → izlaz, bez traya, S-036)` s 2–5 redaka: `window.emit` traži trait `tauri::Emitter` u opsegu (Tauri 2 dijeli metode po traitovima — `Manager` za stanje, `Emitter` za događaje); `prevent_close` + događaj umjesto `hide()`.

`Cargo.toml` korijena: `tauri = { version = "2.11.5", features = ["image-png"] }`. Obriši `tray.rs` i `icons/tray.png` (`git rm`). Provjeri da `tauri.conf.json` nema `trayIcon` (nema ga — ikona se gradila u kodu).

- [ ] **Step 4: Cargo brane**

Run: `cargo build -p sokratis-desktop` · `cargo clippy --workspace --all-targets -- -D warnings` · `cargo fmt --check` · `cargo test --workspace`
Expected: zeleno; `grep -rn "tray" apps/desktop/src-tauri/src` = samo komentari koji objašnjavaju uklanjanje (ili ništa).

- [ ] **Step 5: Dijalog u Svelteu + ožičenje + i18n**

`apps/desktop/src/lib/shell/ConfirmQuit.svelte`:

```svelte
<script lang="ts">
  // ZAŠTO OVAKO (cigla M2/45 — upit pri zatvaranju, S-036)
  // Vlastiti dijalog (ne `tauri-plugin-dialog::ask`) jer natpis, gumbi i boje idu kroz rječnik i
  // tokene kao ostatak sučelja; `role="dialog" aria-modal="true"` + fokus na „Odustani" pri otvaranju
  // (sigurniji gumb dobiva Enter), `Esc` = odustani. Ništa se ovdje ne odlučuje — roditelj (`App.svelte`)
  // zove `api.quit()` na potvrdu.
  import { t } from '../i18n/index.svelte';

  let { onconfirm, oncancel }: { onconfirm: () => void; oncancel: () => void } = $props();
  let cancelButton = $state<HTMLButtonElement | null>(null);

  $effect(() => {
    cancelButton?.focus();
  });

  function onKeydown(e: KeyboardEvent): void {
    if (e.key === 'Escape') oncancel();
  }
</script>

<svelte:window onkeydown={onKeydown} />

<div class="fixed inset-0 z-50 grid place-items-center bg-ink-0/40" role="presentation">
  <div
    role="dialog"
    aria-modal="true"
    aria-labelledby="quit-title"
    class="w-[420px] max-w-[90vw] rounded-lg border border-line bg-surface-1 p-5 shadow-e2"
  >
    <h2 id="quit-title" class="text-lg font-semibold text-ink-0">{t('quit.title')}</h2>
    <p class="mt-2 text-sm text-ink-1">{t('quit.body')}</p>
    <div class="mt-5 flex justify-end gap-2">
      <button
        type="button"
        bind:this={cancelButton}
        class="rounded-md px-3 py-1.5 text-sm text-ink-1 hover:bg-surface-2"
        onclick={oncancel}
      >
        {t('quit.cancel')}
      </button>
      <button
        type="button"
        class="rounded-md bg-brand-500 px-3 py-1.5 text-sm font-medium text-on-brand hover:bg-brand-600"
        onclick={onconfirm}
      >
        {t('quit.confirm')}
      </button>
    </div>
  </div>
</div>
```

`App.svelte`: uvoz `ConfirmQuit`, stanje `let quitOpen = $state(false);`, u `onMount` iza pretplate na `report_updated`:

```ts
    unsubscribeClose = api.onCloseRequested(() => {
      quitOpen = true;
    });
```

(`let unsubscribeClose: (() => void) | null = null;` uz ostale; `onDestroy` ga zove.) Na dno korijenskog `<div>` (iza `<ExplainCard />`):

```svelte
  {#if quitOpen}
    <ConfirmQuit onconfirm={() => void api.quit()} oncancel={() => (quitOpen = false)} />
  {/if}
```

i18n (oba jezika, iste ključeve):

| ključ | hr | en |
|---|---|---|
| `quit.title` | Zatvoriti Sokratis? | Close Sokratis? |
| `quit.body` | Nadzor projekata i obavijesti staju dok ga ponovno ne pokreneš. | Project monitoring and notifications stop until you start it again. |
| `quit.confirm` | Zatvori | Close |
| `quit.cancel` | Odustani | Cancel |
| `settings.autostart.hint` (izmjena) | Sokratis se otvara pri prijavi u Windows. | Sokratis opens when you sign in to Windows. |

- [ ] **Step 6: Brane sučelja**

Run u `apps/desktop`: `npm run check` · `npm run build`
Expected: zeleno (i18n hr = en, vitest uklj. dva nova testa, svelte-check 0 grešaka).

- [ ] **Step 7: Commit (dva commita: Rust pa sučelje)**

```bash
git add Cargo.toml Cargo.lock apps/desktop/src-tauri
git commit -m "M2/45: X = prevent_close + close_requested, naredba quit, tray.rs i tray-icon uklonjeni, set_autostart u autostart.rs -- S-036: svako pokretanje je nov proces"
git add apps/desktop/src apps/desktop/tests
git commit -m "M2/45: ConfirmQuit dijalog (HR/EN, Esc, fokus na Odustani), Api.quit/onCloseRequested, natpis autostarta -- upit prije izlaza umjesto skrivanja u tray (S-036)"
```

**Orkestrator nakon spajanja DESKTOP-2 (ne graditelj):** dimni test u `tauri dev` (identifikator `dev.sokratis.app.dev`): X → dijalog → Odustani (prozor ostaje) → X → Zatvori (proces izlazi, `tasklist` ga nema) → ponovno pokretanje → splash → glavni prozor; osvježenje bez bljeska konzole; tray-ikone nema; `Postavke → autostart` upiše/izbriše registraciju (`autostart:allow-is-enabled`). Čuvar A: `TESTING.md` §5 (X sakriva → X pita i izlazi; tray → nema), `ARCHITECTURE.md` §1 tablica desktopa (bez `tray.rs`, + `autostart.rs`), `RUST.md` §2 pravilo `git_command`.

---
## Tok JEZGRA-2 — grane i dnevnik u jezgri (spec §1.2, §2)

### Task 46: `BranchScope`, `BranchStats`, `CommitRow.branch`, `branch_stats` — sati po udjelu (spec §1.2, S-032)

**Files:**
- Modify: `crates/sokratis-core/src/model.rs` (`CommitRow`, `Report`, `ReportInput`, novi `BranchScope`, `BranchStats`)
- Modify: `crates/sokratis-core/src/metrics/kinds.rs` (`commit_rows` prima kartu grana), `crates/sokratis-core/src/metrics/mod.rs`
- Create: `crates/sokratis-core/src/metrics/branches.rs`
- Modify: `crates/sokratis-core/src/report.rs` (`build_report` + `tests::input()`)
- Modify (samo redci konstrukcije): `crates/sokratis-core/tests/parity.rs`, `crates/sokratis-core/tests/snapshot.rs`, `crates/sokratis-io/src/project.rs` (`input_with`, `Ok(ReportInput { … })`), `crates/sokratis-cli/src/table.rs` (testovi koji grade `Report`), `apps/desktop/src/lib/types.ts`, `apps/desktop/tests/types.test.ts`
- Modify (namjerno): `crates/sokratis-core/tests/snapshots/snapshot__report-sokratstudy-2026-09-17.snap`
- Test: `crates/sokratis-core/tests/branches.rs` (nov)

**Interfaces:**
- Consumes: `Commit`, `CommitRow`, `DayStats`, `BranchInfo` (postoje), `hours_per_day`/`day_stats` (postoje).
- Produces:
  ```rust
  // model.rs
  #[derive(Debug, Clone, Copy, PartialEq, Eq, Default, Serialize, Deserialize)]
  pub enum BranchScope { #[default] #[serde(rename = "all")] AllBranches, #[serde(rename = "default")] DefaultBranch }
  #[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
  pub struct BranchStats { pub name: String, pub commits: u32, pub lines: u64, pub hours: f64, pub merged: bool }
  // CommitRow += pub branch: String            (ZADNJE polje)
  // Report    += pub scope: BranchScope         (iza `branch`)   · pub branches: Vec<BranchStats> (iza `kinds`)
  // ReportInput += pub scope: BranchScope       (iza `branch`)   · pub commit_branches: HashMap<String, String>
  // metrics/kinds.rs
  pub fn commit_rows(commits: &[Commit], overrides: &HashMap<String, WorkKind>, p: &Patterns,
                     commit_branches: &HashMap<String, String>, default_branch: &str) -> Vec<CommitRow>
  // metrics/branches.rs
  pub fn branch_stats(rows: &[CommitRow], commits: &[Commit], days: &[DayStats],
                      branches: &[BranchInfo], default_branch: &str) -> Vec<BranchStats>
  ```
  JSON: `"scope": "all" | "default"`; `"branches": [{ "name", "commits", "lines", "hours", "merged" }]`; svaki redak `commits[]` nosi `"branch"`.

- [ ] **Step 1: Padajući test — `tests/branches.rs`**

```rust
//! ZAŠTO RUST OVAKO (cigla M2/46 — grane u jezgri, S-032)
//! Integracijski test: jezgra vidi SAMO tekst loga + kartu `sha → grana` (`ReportInput`), nikad git.
//! Fixture je inline: četiri commita, dvije grane izvan zadane, jedna s `|` u imenu — redak
//! `%h|%S` iz `io` se dijeli na PRVOM `|`, pa ime s `|` mora preživjeti (Review Focus #3).
use sokratis_core::{BranchInfo, BranchScope, Profile, ReportInput, build_report};
use std::collections::HashMap;

// Dan 1 (2026-09-10): a1 na main u 08:00, b2 na feat/x u 09:30 (razmak 1,5 h < 2 h → +1,5)
// Dan 2 (2026-09-11): c3 na feat/x u 08:00 (nova sesija +0,5), d4 na "y|z" u 09:30 (+1,5)
// → sati dana: 2,0 i 2,0; po udjelu: main 1,0 · feat/x 2,0 · y|z 1,0 (zbroj = 4,0 = ukupno).
const LOG: &str = "\
@@a1|1789020000|1789020000|2026-09-10|2026-09-10|F1/1 kod na mainu\n1\t0\tjs/a.js\n\n\
@@b2|1789025400|1789025400|2026-09-10|2026-09-10|F1/2 kod u grani\n2\t0\tjs/b.js\n\n\
@@c3|1789106400|1789106400|2026-09-11|2026-09-11|fix: kvar u grani\n3\t1\tjs/c.js\n\n\
@@d4|1789111800|1789111800|2026-09-11|2026-09-11|docs: zapis u drugoj grani\n4\t0\tdocs/records/PROGRESS.md\n";

fn input(scope: BranchScope, map: &[(&str, &str)]) -> ReportInput {
    ReportInput {
        git_log: LOG.into(),
        diary: None,
        plan: None,
        docs: vec![],
        branches: vec![
            BranchInfo { name: "feat/x".into(), last_commit_time: 1789106400, ahead_of_default: 2, merged: false },
            BranchInfo { name: "y|z".into(), last_commit_time: 1789111800, ahead_of_default: 1, merged: false },
        ],
        overrides: HashMap::new(),
        visions: vec![],
        now: 1789300000,
        today: "2026-09-12".into(),
        since: "2026-09-01".into(),
        until: None,
        branch: "main".into(),
        scope,
        commit_branches: map.iter().map(|(s, b)| (s.to_string(), b.to_string())).collect(),
    }
}

#[test]
fn rows_carry_branch_and_stats_split_hours_by_commit_share() {
    let r = build_report(
        &input(BranchScope::AllBranches, &[("b2", "feat/x"), ("c3", "feat/x"), ("d4", "y|z")]),
        &Profile::default(),
    )
    .unwrap();
    assert_eq!(r.scope, BranchScope::AllBranches);
    let by_sha: HashMap<&str, &str> = r.commits.iter().map(|c| (c.sha.as_str(), c.branch.as_str())).collect();
    assert_eq!(by_sha["a1"], "main", "commit izvan karte je zadana grana");
    assert_eq!(by_sha["b2"], "feat/x");
    assert_eq!(by_sha["d4"], "y|z", "ime grane s | preživi");
    let names: Vec<&str> = r.branches.iter().map(|b| b.name.as_str()).collect();
    assert_eq!(names, ["main", "feat/x", "y|z"], "zadana prva, ostale po commitima silazno pa po imenu");
    let feat = &r.branches[1];
    assert_eq!((feat.commits, feat.lines, feat.merged), (2, 6, false));
    assert_eq!(feat.hours, 2.0);
    assert_eq!(r.branches[0].hours, 1.0);
    assert_eq!(r.branches[2].hours, 1.0);
    assert!(r.branches[0].merged, "zadana grana je uvijek 'spojena'");
    let total: f64 = r.days.iter().map(|d| d.hours).sum();
    let split: f64 = r.branches.iter().map(|b| b.hours).sum();
    assert!((total - split).abs() < 1e-9, "zbroj po granama = ukupni sati");
}

#[test]
fn single_branch_gives_one_row() {
    let r = build_report(&input(BranchScope::DefaultBranch, &[]), &Profile::default()).unwrap();
    assert_eq!(r.scope, BranchScope::DefaultBranch);
    assert!(r.commits.iter().all(|c| c.branch == "main"));
    assert_eq!(r.branches.len(), 1);
    assert_eq!(r.branches[0].commits, 4);
}

#[test]
fn empty_commits_give_no_rows() {
    let mut i = input(BranchScope::AllBranches, &[]);
    i.since = "2026-09-20".into();
    let r = build_report(&i, &Profile::default()).unwrap();
    assert!(r.commits.is_empty());
    assert!(r.branches.is_empty());
}

#[test]
fn scope_and_branches_are_in_json() {
    let r = build_report(&input(BranchScope::AllBranches, &[("b2", "feat/x")]), &Profile::default()).unwrap();
    let v: serde_json::Value = serde_json::to_value(&r).unwrap();
    assert_eq!(v["scope"], "all");
    assert_eq!(v["branches"][0]["name"], "main");
    assert_eq!(v["commits"][1]["branch"], "feat/x");
}
```

(`serde_json` je već dev-ovisnost jezgre — `tests/parity.rs` ga koristi.)

- [ ] **Step 2: Pokreni — mora pasti na kompilaciji**

Run: `cargo test -p sokratis-core --test branches`
Expected: FAIL — `BranchScope` ne postoji, `ReportInput` nema polja `scope`/`commit_branches`.

- [ ] **Step 3: Model**

`model.rs` — iza `BranchInfo`:

```rust
/// Koje su grane ušle u mjerenje (S-032). JSON `"all"` / `"default"` — isti tekst u profilu
/// (`branch_scope`) i u `Report.scope`, pa korisnik čita istu riječ na oba mjesta.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Default, Serialize, Deserialize)]
pub enum BranchScope {
    #[default]
    #[serde(rename = "all")]
    AllBranches,
    #[serde(rename = "default")]
    DefaultBranch,
}

/// Jedan red sekcije Grane: commiti i redci su zbrojeni po oznaci grane iz `CommitRow.branch`;
/// `hours` su sati dana podijeljeni po udjelu commita grane tog dana (proxy, S-032) — zbroj po
/// granama je jednak ukupnim satima. `merged` dolazi iz `BranchInfo`; zadana grana je uvijek `true`.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct BranchStats {
    pub name: String,
    pub commits: u32,
    pub lines: u64,
    pub hours: f64,
    pub merged: bool,
}
```

`CommitRow` dobiva zadnje polje `/// grana koja je commit dosegla (S-032); zadana grana ako nije u karti` `pub branch: String,`. `Report`: `pub scope: BranchScope,` odmah iza `pub branch: String,`; `pub branches: Vec<BranchStats>,` iza `pub kinds`. `ReportInput`: iza `pub branch: String,`:

```rust
    /// Što je `io` obišao (S-032) — jezgra to samo prepiše u `Report.scope`.
    pub scope: BranchScope,
    /// `sha → grana` SAMO za commite izvan zadane grane (`git log --branches --not <zadana>
    /// --format=%h|%S`); commit kojeg nema u karti je na zadanoj grani. Prazno kad je
    /// `scope == DefaultBranch`.
    pub commit_branches: HashMap<String, String>,
```

Zaglavlje `model.rs` dobiva odlomak `//! Dopuna (cigla M2/46, S-032): …` — 2–5 redaka: zašto enum s `serde(rename)` (ista riječ u profilu i izvještaju), zašto `HashMap` u ulazu (jezgra ne zna git), zašto `branch` zadnje polje (snapshot mijenja samo dodatkom).

- [ ] **Step 4: `commit_rows` s kartom + `branch_stats`**

`metrics/kinds.rs`:

```rust
pub fn commit_rows(
    commits: &[Commit],
    overrides: &HashMap<String, WorkKind>,
    p: &Patterns,
    commit_branches: &HashMap<String, String>,
    default_branch: &str,
) -> Vec<CommitRow> {
    commits
        .iter()
        .map(|c| CommitRow {
            sha: c.sha.clone(),
            date: c.date.clone(),
            author_time: c.author_time,
            subject: c.subject.clone(),
            kind: effective_kind(c, overrides, p),
            sub: classify_sub(&c.subject, p),
            overridden: overrides.contains_key(&c.sha),
            // `get(..).cloned()` + `unwrap_or_else`: commit izvan karte je na zadanoj grani (S-032).
            branch: commit_branches
                .get(&c.sha)
                .cloned()
                .unwrap_or_else(|| default_branch.to_string()),
        })
        .collect()
}
```

(dopuni zaglavlje `kinds.rs` s 2 retka o M2/46). Postojeći unit-testovi u `kinds.rs` koji zovu `commit_rows` dobivaju `&HashMap::new(), "main"`.

`metrics/branches.rs` (novo):

```rust
//! ZAŠTO RUST OVAKO (cigla M2/46 — grane, S-032)
//! `rows.iter().zip(commits)` spaja dva paralelna niza (isti redoslijed, ista dužina — `commit_rows`
//! to jamči) bez indeksa. `BTreeMap<&str, _>` s POSUĐENIM ključem drži grane sortirane po imenu bez
//! klona dok traje zbrajanje; tek `BranchStats` posjeduje `String`. Sati po grani su proxy: sati DANA
//! (`DayStats.hours`, iz sesija S-007) × udio commita grane tog dana — zbroj po granama je uvijek
//! jednak ukupnom, za razliku od sesija po grani (dvije grane istodobno = sati dvaput).
use crate::{BranchInfo, BranchStats, Commit, CommitRow, DayStats};
use std::collections::{BTreeMap, HashMap, HashSet};

#[derive(Default)]
struct Acc {
    commits: u32,
    lines: u64,
}

pub fn branch_stats(
    rows: &[CommitRow],
    commits: &[Commit],
    days: &[DayStats],
    branches: &[BranchInfo],
    default_branch: &str,
) -> Vec<BranchStats> {
    let mut acc: BTreeMap<&str, Acc> = BTreeMap::new();
    let mut per_day: HashMap<(&str, &str), u32> = HashMap::new();
    for (r, c) in rows.iter().zip(commits) {
        let a = acc.entry(r.branch.as_str()).or_default();
        a.commits += 1;
        a.lines += c.files.iter().map(|f| f.added + f.deleted).sum::<u64>();
        *per_day.entry((r.branch.as_str(), r.date.as_str())).or_default() += 1;
    }
    let merged: HashSet<&str> = branches
        .iter()
        .filter(|b| b.merged)
        .map(|b| b.name.as_str())
        .collect();
    let mut out: Vec<BranchStats> = acc
        .into_iter()
        .map(|(name, a)| {
            let hours: f64 = days
                .iter()
                .map(|d| {
                    let on_branch = per_day.get(&(name, d.date.as_str())).copied().unwrap_or(0);
                    if d.commits == 0 {
                        0.0
                    } else {
                        d.hours * f64::from(on_branch) / f64::from(d.commits)
                    }
                })
                .sum();
            BranchStats {
                name: name.to_string(),
                commits: a.commits,
                lines: a.lines,
                hours: (hours * 10.0).round() / 10.0,
                merged: name == default_branch || merged.contains(name),
            }
        })
        .collect();
    // Po commitima silazno, pa po imenu (stabilno); zadana grana ide na vrh ako ima commita.
    out.sort_by(|a, b| b.commits.cmp(&a.commits).then_with(|| a.name.cmp(&b.name)));
    if let Some(pos) = out.iter().position(|b| b.name == default_branch)
        && pos != 0
    {
        let d = out.remove(pos);
        out.insert(0, d);
    }
    out
}
```

`metrics/mod.rs`: `pub mod branches;` + `pub use branches::branch_stats;`.

`report.rs`: `let rows = commit_rows(&commits, &input.overrides, &p, &input.commit_branches, &input.branch);` · nakon `let days = …` i `let rows = …`: `let branches = branch_stats(&rows, &commits, &days, &input.branches, &input.branch);` · u `Report { … scope: input.scope, … branches, … }`. `report::tests::input()` dobiva `scope: BranchScope::DefaultBranch, commit_branches: HashMap::new()`.

- [ ] **Step 5: Redci konstrukcije u drugim crateovima i TS zrcalo**

- `tests/parity.rs`, `tests/snapshot.rs`: u `ReportInput { … }` dodaj `scope: BranchScope::DefaultBranch, commit_branches: HashMap::new(),` (+ `use sokratis_core::BranchScope`).
- `crates/sokratis-io/src/project.rs::input_with`: u `Ok(ReportInput { … })` dodaj `scope: BranchScope::DefaultBranch, commit_branches: HashMap::new(),` s komentarom `// M2/46: privremeno; IO-2 (T49) puni iz profila i gita.` (+ `use sokratis_core::BranchScope`).
- `crates/sokratis-cli/src/table.rs` testovi koji grade `Report`: dodaj `scope: BranchScope::DefaultBranch, branches: vec![],`.
- `apps/desktop/src/lib/types.ts`: `export type BranchScope = 'all' | 'default';` · `export interface BranchStats { name: string; commits: number; lines: number; hours: number; merged: boolean }` · `CommitRow` += `branch: string;` (zadnje) · `Report` += `scope: BranchScope;` iza `branch`, `branches: BranchStats[];` iza `kinds`.
- `apps/desktop/tests/types.test.ts`: popis ključeva postaje (abecedno) `['branch','branches','commits','days','deliveries','docs','generated_at','indicators','kinds','phases','scope','signals','since','touched','until','vision_totals','visions']`; dodaj `expect(r.scope).toBe('default'); expect(r.branches[0]).toMatchObject({ name: 'main', merged: true });`.

- [ ] **Step 6: Snapshot namjerno, pa sve brane**

Run: `INSTA_UPDATE=always cargo test -p sokratis-core --test snapshot` → `git diff crates/sokratis-core/tests/snapshots/` pokazuje SAMO: `"scope": "default"`, `"branches": [ { "name": "main", "commits": 190, … "merged": true } ]`, `"branch": "main"` u svakom retku `commits`. Zatim:
`cargo test --workspace` · `cargo clippy --workspace --all-targets -- -D warnings` · `cargo fmt --check` · u `apps/desktop`: `npm run check` (types.test, explain.test, i18n.test čitaju isti snapshot).
Expected: sve zeleno; paritet nepromijenjen.

- [ ] **Step 7: Commit**

```bash
git add crates apps/desktop/src/lib/types.ts apps/desktop/tests/types.test.ts
git commit -m "M2/46: BranchScope, CommitRow.branch, Report.scope/branches, branch_stats (sati po udjelu commita) -- snapshot mijenjan NAMJERNO: +scope, +branches, +branch po retku (S-032)"
```

---

### Task 47: Unija dnevnika iz više stabala; `Touched.worktrees/diaries` (spec §2, S-033)

**Files:**
- Modify: `crates/sokratis-core/src/model.rs` (`ReportInput.diary` → `diaries: Vec<String>`, `+ worktrees: u32`; `Touched` += `worktrees`, `diaries`)
- Modify: `crates/sokratis-core/src/parse/diary.rs` (+ `parse_diaries`), `crates/sokratis-core/src/parse/mod.rs` (izvoz)
- Modify: `crates/sokratis-core/src/report.rs`
- Modify (samo redci konstrukcije): `tests/parity.rs`, `tests/snapshot.rs`, `tests/branches.rs`, `report.rs::tests::input()`, `crates/sokratis-io/src/project.rs`, `apps/desktop/src/lib/types.ts`, `apps/desktop/tests/types.test.ts`
- Modify (namjerno): snapshot
- Test: `crates/sokratis-core/tests/diary_union.rs` (nov)

**Interfaces:**
- Produces: `pub fn parse_diaries(texts: &[String], p: &Patterns, since: &str) -> Vec<Delivery>` (`parse/diary.rs`, izvezeno iz `parse`); `ReportInput.diaries: Vec<String>` (redoslijed = od vodećeg stabla prema starijima — `io` to jamči, T50), `ReportInput.worktrees: u32`; `Touched { …, worktrees: u32, diaries: u32 }`.

- [ ] **Step 1: Padajući test — `tests/diary_union.rs`**

```rust
//! ZAŠTO RUST OVAKO (cigla M2/47 — unija dnevnika, S-033)
//! Dva teksta dnevnika (vodeće stablo prvo, pa `main`) s jednim preklopljenim naslovom: unija po
//! (datum, naslov), PRVI viđeni pobjeđuje — pa `model` iz vodećeg stabla ostaje. Test ide kroz
//! `build_report` (javni ugovor), ne kroz `parse_diaries` izravno, jer je `Touched.diaries` dio
//! izlaza i mora se vidjeti izvana.
use sokratis_core::{BranchScope, Profile, ReportInput, build_report};
use std::collections::HashMap;

const LEAD: &str = "\
## 2026-09-20 (FABLE) — F6/1 MCP most\n\n\
## 2026-09-21 (FABLE) — F6/2 čitanje polica 🚀\n\n\
## 2026-09-22 (SONNET) — docs: zapis F6\n";
const MAIN: &str = "\
## 2026-09-13 (FABLE) — F2/9 zid gotov\n\n\
## 2026-09-20 (OPUS) — F6/1 MCP most\n";

fn input(diaries: Vec<String>) -> ReportInput {
    ReportInput {
        git_log: "@@a1|1789020000|1789020000|2026-09-10|2026-09-10|F1/1 kod\n1\t0\tjs/a.js\n".into(),
        diaries,
        plan: None,
        docs: vec![],
        branches: vec![],
        overrides: HashMap::new(),
        visions: vec![],
        now: 1789300000,
        today: "2026-09-23".into(),
        since: "2026-09-01".into(),
        until: None,
        branch: "main".into(),
        scope: BranchScope::AllBranches,
        commit_branches: HashMap::new(),
        worktrees: 2,
    }
}

#[test]
fn deliveries_are_united_by_date_and_title_first_wins() {
    let r = build_report(&input(vec![LEAD.into(), MAIN.into()]), &Profile::default()).unwrap();
    let titles: Vec<(&str, &str)> = r.deliveries.iter().map(|d| (d.date.as_str(), d.title.as_str())).collect();
    assert_eq!(
        titles,
        [
            ("2026-09-13", "F2/9 zid gotov"),
            ("2026-09-20", "F6/1 MCP most"),
            ("2026-09-21", "F6/2 čitanje polica 🚀"),
            ("2026-09-22", "docs: zapis F6"),
        ],
        "4 isporuke: preklop (20. 9.) jednom, sortirano po datumu"
    );
    let overlap = r.deliveries.iter().find(|d| d.date == "2026-09-20").unwrap();
    assert_eq!(overlap.model, "FABLE", "prvi tekst (vodeće stablo) pobjeđuje");
    assert_eq!((r.touched.worktrees, r.touched.diaries), (2, 2));
}

#[test]
fn no_diaries_means_no_deliveries_and_zero_touched() {
    let r = build_report(&input(vec![]), &Profile::default()).unwrap();
    assert!(r.deliveries.is_empty());
    assert_eq!(r.touched.diaries, 0);
}
```

Run: `cargo test -p sokratis-core --test diary_union` → FAIL (nema `diaries`, `worktrees`).

- [ ] **Step 2: Model + parser + sastavljanje**

`model.rs`: u `ReportInput` zamijeni `pub diary: Option<String>,` s

```rust
    /// Tekst dnevnika iz SVAKOG radnog stabla, od vodećeg prema starijima (S-033); prazno = nema
    /// dnevnika. Unija po (datum, naslov) je u `parse::parse_diaries`.
    pub diaries: Vec<String>,
```

i dodaj (zadnje polje) `/// broj radnih stabala koje je `io` obišao — u `Touched` (mjerač kaže koliko je dotaknuo)` `pub worktrees: u32,`. `Touched` += `pub worktrees: u32, pub diaries: u32,` (zadnja dva polja).

`parse/diary.rs`:

```rust
/// Unija isporuka iz više dnevnika (S-033): svaki tekst prolazi `parse_diary`, zatim se redci
/// spajaju po ključu (datum, naslov) — PRVI viđeni pobjeđuje (pozivatelj šalje vodeće stablo prvo),
/// pa `model`/`deploy` uređeni u grani u kojoj se radi nadjačaju `main`. Ishod je sortiran po
/// datumu, stabilno (kao `parse_diary`).
pub fn parse_diaries(texts: &[String], p: &Patterns, since: &str) -> Vec<Delivery> {
    let mut seen: HashSet<(String, String)> = HashSet::new();
    let mut out: Vec<Delivery> = texts
        .iter()
        .flat_map(|t| parse_diary(t, p, since))
        .filter(|d| seen.insert((d.date.clone(), d.title.clone())))
        .collect();
    out.sort_by(|a, b| a.date.cmp(&b.date));
    out
}
```

(`use std::collections::HashSet;` + unit test u istoj datoteci: dva teksta, preklop, `len == 4`.) `parse/mod.rs`: izvezi `parse_diaries` uz `parse_diary`.

`report.rs`: `let deliveries: Vec<_> = parse_diaries(&input.diaries, &p, &input.since).into_iter().filter(|d| …until…).collect();` · `Touched { …, worktrees: input.worktrees, diaries: input.diaries.len() as u32 }`.

- [ ] **Step 3: Redci konstrukcije**

`parity.rs`/`snapshot.rs`: `diaries: vec![fx("PROGRESS.md")], worktrees: 1,` · `tests/branches.rs`, `report::tests::input()`: `diaries: vec![], worktrees: 1,` (ili `diaries: vec![tekst]` gdje je test imao `diary: Some`) · `io/src/project.rs`: `diaries: read_opt(&self.profile.diary_path).into_iter().collect(), worktrees: 1,` s komentarom `// M2/47: privremeno jedno stablo; IO-2 (T50) čita sva.` · `types.ts`: `Touched` += `worktrees: number; diaries: number;` · `types.test.ts`: `expect(r.touched).toMatchObject({ worktrees: 1, diaries: 1 })`.

- [ ] **Step 4: Snapshot namjerno + brane**

`INSTA_UPDATE=always cargo test -p sokratis-core --test snapshot` → diff SAMO `touched.worktrees: 1`, `touched.diaries: 1`. `cargo test --workspace` · clippy · fmt · `npm run check`. Paritet nepromijenjen (isti tekst, jedan dnevnik).

- [ ] **Step 5: Commit**

```bash
git add crates apps/desktop/src/lib/types.ts apps/desktop/tests/types.test.ts
git commit -m "M2/47: ReportInput.diaries (unija po datum+naslov, prvi pobjeduje) i Touched.worktrees/diaries -- snapshot mijenjan NAMJERNO: +touched.worktrees, +touched.diaries (S-033)"
```

---

### Task 48: Profil `branch_scope` (zadano `all`) (spec §1.3, S-032)

**Files:**
- Modify: `crates/sokratis-core/src/profile.rs` (polje, `Default`, testovi)

**Interfaces:**
- Produces: `Profile.branch_scope: BranchScope` (zadano `BranchScope::AllBranches`); JSON `"branch_scope": "all" | "default"`. IO-2 (T49) ga čita u `input_with`.

- [ ] **Step 1: Padajući testovi (u `profile.rs` `mod tests`)**

```rust
    #[test]
    fn branch_scope_defaults_to_all_and_parses_both_values() {
        assert_eq!(Profile::default().branch_scope, BranchScope::AllBranches);
        let p: Profile = serde_json::from_str(r#"{ "branch_scope": "default" }"#).unwrap();
        assert_eq!(p.branch_scope, BranchScope::DefaultBranch);
        let p: Profile = serde_json::from_str(r#"{ "branch_scope": "all" }"#).unwrap();
        assert_eq!(p.branch_scope, BranchScope::AllBranches);
    }

    #[test]
    fn branch_scope_rejects_unknown_value_naming_the_allowed_ones() {
        let err = serde_json::from_str::<Profile>(r#"{ "branch_scope": "worktrees" }"#).unwrap_err();
        let msg = err.to_string();
        assert!(msg.contains("`all`") && msg.contains("`default`"), "{msg}");
    }
```

Run: `cargo test -p sokratis-core --lib branch_scope` → FAIL (nema polja).

- [ ] **Step 2: Polje**

`profile.rs`: `use crate::model::{BranchScope, WorkKind};` · u `struct Profile` iza `default_branch`:

```rust
    /// Koje grane ulaze u metrike (S-032): `"all"` = sve lokalne grane (zadano — prvi korisnik radi
    /// u granama), `"default"` = samo `default_branch` (paritet s `RAD.xlsx`, ponašanje do 1.0.0-pre).
    pub branch_scope: BranchScope,
```

`Default`: `branch_scope: BranchScope::AllBranches,`. Zaglavlje: 2 retka o tome da je enum dijeljen s `Report` pa serde sam odbija nepoznatu vrijednost s popisom (nema ručne `validate`).

- [ ] **Step 3: Brane**

`cargo test --workspace` · clippy · fmt. Paritet i snapshot nepromijenjeni (profil ne ulazi u `Report`). **Napomena za izvještaj cigle:** kanonski JSON profila (`engine.rs::try_snapshot`) sad sadrži `branch_scope`, pa prva snimka nakon nadogradnje dobiva nov `profile_seen_id` — trend to označi kao promjenu profila (spec §1.3), bez koda.

- [ ] **Step 4: Commit**

```bash
git add crates/sokratis-core/src/profile.rs
git commit -m "M2/48: Profile.branch_scope (all | default, zadano all) -- dopuna S-005: zadano mjerenje vise nije samo zadana grana (S-032)"
```

**Orkestrator nakon spajanja JEZGRE-2:** čuvar A — `ARCHITECTURE.md` §2 (korak 6 `commit_rows` s kartom, nov korak `branch_stats`, `parse_diaries`), §3 (`Report`: 17 polja — `scope`, `branches`; `CommitRow.branch`; `Touched` 6 polja), tablica polja profila (`branch_scope`); `RUST.md` §4 (`f64::from(u32)`, `HashSet::insert` kao filtar, let-chain uz `position`).

---
## Tok IO-2 — git nad svim granama, stabla, CLI (spec §1.1, §1.3, §2)

### Task 49: `Scope` u `GitSource`, `commit_sources`, `cached_log(scope)`, `input_with` puni kartu (spec §1.1, S-032)

**Files:**
- Modify: `crates/sokratis-io/src/git.rs` (trait `GitSource`: `log`, `rev_list`, nov `commit_sources`; `GitCli` impl; nov `Scope`)
- Modify: `crates/sokratis-io/src/cache.rs` (`cached_log(git, cache, scope, since, until)`; lažni `GitSource` u testovima)
- Modify: `crates/sokratis-io/src/project.rs` (`input_with`: `Scope` iz profila, `commit_branches`, `scope`)
- Modify: `crates/sokratis-io/src/lib.rs` (`pub use git::{GitCli, GitSource, Scope}`)
- Modify: `crates/sokratis-io/tests/git_cli.rs`, `tests/cache.rs`, `tests/project.rs`, `tests/perf.rs`
- Modify (pozivatelji `log`/`rev_list` izvan `io`): nema ih — `commands.rs` zove `input_cached`, CLI `input_between`. Provjeri `grep -rn "\.log(\|\.rev_list(" crates apps --include=*.rs`.

**Interfaces:**
- Consumes: `Profile.branch_scope` (T48), `BranchScope` (T46).
- Produces:
  ```rust
  /// Koje reference git obilazi (S-032): jedna grana ili sve lokalne (`--branches`: bez remote-tracking
  /// referenci i tagova; svaki commit jednom).
  #[derive(Debug, Clone, Copy, PartialEq, Eq)]
  pub enum Scope<'a> { Branch(&'a str), AllBranches }
  // GitSource
  fn log(&self, scope: Scope<'_>, since: &str, until: Option<&str>) -> Result<String, IoError>;
  fn rev_list(&self, scope: Scope<'_>, since: &str, until: Option<&str>) -> Result<Vec<String>, IoError>;
  /// `sha(%h) → grana(%S)` SAMO za commite izvan `default_ref` (`git log --branches --not <default_ref>`).
  fn commit_sources(&self, default_ref: &str, since: &str, until: Option<&str>) -> Result<HashMap<String, String>, IoError>;
  // cache.rs
  pub fn cached_log(git: &dyn GitSource, cache: &dyn CommitCache, scope: Scope<'_>, since: &str, until: Option<&str>) -> Result<String, IoError>;
  ```

- [ ] **Step 1: Padajući testovi — `tests/git_cli.rs`**

```rust
/// M2/49 (S-032): `--branches` obiđe SVE lokalne grane, svaki commit jednom; `commit_sources` daje
/// granu SAMO za commite izvan zadane; ime grane s `|` preživi (`splitn(2, '|')` — SHA nema `|`).
#[test]
fn all_branches_scope_sees_feature_commits_and_commit_sources_labels_them() {
    let r = Repo::init();
    let a = r.commit("a.txt", "1", "na mainu", "2026-09-10T10:00:00+02:00", "2026-09-10T10:00:00+02:00");
    r.git(&["checkout", "-q", "-b", "feat/x"]);
    let b = r.commit("b.txt", "2", "u grani", "2026-09-11T10:00:00+02:00", "2026-09-11T10:00:00+02:00");
    r.git(&["checkout", "-q", "-b", "y|z", "main"]);
    let c = r.commit("c.txt", "3", "u grani s crtom", "2026-09-12T10:00:00+02:00", "2026-09-12T10:00:00+02:00");
    r.git(&["checkout", "-q", "main"]);
    let g = GitCli::new(r.path());

    let only_main = g.log(Scope::Branch("main"), "2026-09-01", None).unwrap();
    assert!(only_main.contains(&a) && !only_main.contains(&b) && !only_main.contains(&c));

    let all = g.log(Scope::AllBranches, "2026-09-01", None).unwrap();
    assert!(all.contains(&a) && all.contains(&b) && all.contains(&c));
    assert_eq!(all.matches("@@").count(), 3, "svaki commit točno jednom");

    let revs = g.rev_list(Scope::AllBranches, "2026-09-01", None).unwrap();
    assert_eq!(revs.len(), 3);

    let map = g.commit_sources("main", "2026-09-01", None).unwrap();
    assert_eq!(map.get(&b).map(String::as_str), Some("feat/x"));
    assert_eq!(map.get(&c).map(String::as_str), Some("y|z"), "ime grane s | preživi");
    assert!(!map.contains_key(&a), "commit na zadanoj grani nije u karti");
}

#[test]
fn commit_sources_is_empty_when_only_default_branch_exists() {
    let r = Repo::init();
    r.commit("a.txt", "1", "sam", "2026-09-10T10:00:00+02:00", "2026-09-10T10:00:00+02:00");
    let g = GitCli::new(r.path());
    assert!(g.commit_sources("main", "2026-09-01", None).unwrap().is_empty());
    assert_eq!(g.log(Scope::AllBranches, "2026-09-01", None).unwrap().matches("@@").count(), 1);
}
```

Postojeći pozivi `g.log("main", …)` u `git_cli.rs` postaju `g.log(Scope::Branch("main"), …)`; `use sokratis_io::{GitCli, GitSource, IoError, Project, Scope};`.

Run: `cargo test -p sokratis-io --test git_cli` → FAIL na kompilaciji (`Scope` ne postoji).

- [ ] **Step 2: `Scope` + trait + `GitCli`**

`git.rs` — iznad `pub trait GitSource`:

```rust
/// Koje reference `log`/`rev_list` obilaze (S-032). `Branch(ime)` je današnje ponašanje (paritet,
/// `--scope default`); `AllBranches` je `--branches`: sve lokalne grane, svaki commit JEDNOM (git ga
/// dedupira po SHA-i), bez remote-tracking referenci i tagova. Lifetime `'a` posuđuje ime grane od
/// pozivatelja (`&self.profile.default_branch`) umjesto da ga klonira za svaki poziv.
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum Scope<'a> {
    Branch(&'a str),
    AllBranches,
}

impl<'a> Scope<'a> {
    /// Argument revizije koji git razumije: ime grane ili `--branches`.
    fn rev_arg(self) -> &'a str {
        match self {
            Scope::Branch(name) => name,
            Scope::AllBranches => "--branches",
        }
    }
}
```

Trait: `fn log(&self, scope: Scope<'_>, since: &str, until: Option<&str>)`, `fn rev_list(&self, scope: Scope<'_>, …)`, i nova:

```rust
    /// `sha → grana` SAMO za commite izvan `default_ref` (S-032): `git log --branches --not
    /// <default_ref> --format=%h|%S`. `%S` = kratko ime reference kojom je git commit DOSEGAO; commit
    /// dostižan iz dviju nespojenih grana dobiva jednu od njih (gitov obilazak presuđuje). Commit
    /// kojeg nema u karti je na zadanoj grani. Isti prozor (`window_args`) kao `log`/`rev_list`.
    fn commit_sources(
        &self,
        default_ref: &str,
        since: &str,
        until: Option<&str>,
    ) -> Result<HashMap<String, String>, IoError>;
```

`GitCli`: u `log` `let mut args = vec!["log", scope.rev_arg(), &since_arg];`, u `rev_list` `vec!["rev-list", scope.rev_arg(), &since_arg]`; nova metoda:

```rust
    fn commit_sources(
        &self,
        default_ref: &str,
        since: &str,
        until: Option<&str>,
    ) -> Result<HashMap<String, String>, IoError> {
        let (since_arg, until_arg) = window_args(since, until);
        // `--not` negira SVE reference iza sebe do kraja: `--branches` mora doći PRIJE njega.
        let mut args = vec!["log", "--branches", "--not", default_ref, &since_arg];
        if let Some(a) = &until_arg {
            args.push(a);
        }
        args.extend(["--format=%h|%S", "--"]);
        Ok(self
            .run(&args)?
            .lines()
            .filter_map(|l| l.split_once('|'))
            .map(|(sha, branch)| (sha.trim().to_string(), branch.trim().to_string()))
            .collect())
    }
```

(`split_once('|')` dijeli na PRVOM `|`, ostatak — i s `|` u imenu — je grana.) Zaglavlje `git.rs`: odlomak `//! Cigla M2/49 (S-032): …` o `Scope<'a>` (enum s lifetimeom umjesto `String`), `--not` redoslijedu, `split_once`.

- [ ] **Step 3: `cached_log(scope)` i lažni `GitSource`**

`cache.rs`: potpis `pub fn cached_log(git: &dyn GitSource, cache: &dyn CommitCache, scope: Scope<'_>, since: &str, until: Option<&str>)`, tijelo `git.rev_list(scope, since, until)?`. Svaki lažni `GitSource` u `cache.rs`/`tests/cache.rs` dobiva `commit_sources` (`Ok(HashMap::new())` ili `unimplemented!("test ga ne zove")`) i nove potpise `log`/`rev_list` (`Scope<'_>` umjesto `&str`; test koji je bilježio `branch` sad bilježi `format!("{scope:?}")`).

- [ ] **Step 4: `input_with` puni `scope` i kartu**

`project.rs::input_with`, nakon izbora `(ref_name, label)`:

```rust
        // S-032: profil bira opseg; `ref_name` je ista referenca kao do sada (zadana → trenutna →
        // HEAD), pa detached HEAD i repo bez zadane grane rade kao prije.
        let scope = match self.profile.branch_scope {
            BranchScope::AllBranches => Scope::AllBranches,
            BranchScope::DefaultBranch => Scope::Branch(&ref_name),
        };
```

`git_log`: `cached_log(&self.git, cache, scope, &fetch_since, until)?` / `self.git.log(scope, &fetch_since, until)?`. Zatim:

```rust
        // Karta `sha → grana` postoji SAMO kad se mjere sve grane — jedan proces više (spec §1.1).
        let commit_branches = match self.profile.branch_scope {
            BranchScope::AllBranches => self.git.commit_sources(&ref_name, &fetch_since, until)?,
            BranchScope::DefaultBranch => HashMap::new(),
        };
```

U `Ok(ReportInput { … scope: self.profile.branch_scope, commit_branches, … })` (makni privremeni komentar iz T46). `lib.rs`: `pub use git::{GitCli, GitSource, Scope};`.

- [ ] **Step 5: Test projekta + perf granica**

`tests/project.rs`:

```rust
/// M2/49: `input()` po zadanom profilu (`branch_scope = all`) vidi commit iz feature-grane i zna mu
/// granu; profil `"branch_scope": "default"` vraća današnje ponašanje (samo `main`, prazna karta).
#[test]
fn input_follows_branch_scope_from_profile() {
    let r = Repo::init();
    r.commit("js/a.js", "1", "F1/1 main", "2026-09-10T10:00:00+02:00", "2026-09-10T10:00:00+02:00");
    r.git(&["checkout", "-q", "-b", "feat/x"]);
    let b = r.commit("js/b.js", "2", "F1/2 grana", "2026-09-11T10:00:00+02:00", "2026-09-11T10:00:00+02:00");
    r.git(&["checkout", "-q", "main"]);

    let p = Project::open(r.path()).unwrap();
    let input = p.input(Some("2026-09-01")).unwrap();
    assert_eq!(input.scope, sokratis_core::BranchScope::AllBranches);
    assert!(input.git_log.contains(&b));
    assert_eq!(input.commit_branches.get(&b).map(String::as_str), Some("feat/x"));

    write(&r, ".sokratis/profile.json", r#"{ "branch_scope": "default" }"#);
    let p = Project::open(r.path()).unwrap();
    let input = p.input(Some("2026-09-01")).unwrap();
    assert_eq!(input.scope, sokratis_core::BranchScope::DefaultBranch);
    assert!(!input.git_log.contains(&b));
    assert!(input.commit_branches.is_empty());
}
```

`tests/perf.rs`: granica `spawned <= 6` (poruka: „dopušteno 6 — +1 `commit_sources` (M2/49)"), ime testa `…spawns_at_most_six_git_processes`.

- [ ] **Step 6: Brane + commit**

`cargo test --workspace` · clippy · fmt.

```bash
git add crates/sokratis-io
git commit -m "M2/49: Scope::AllBranches (git log --branches), commit_sources (%h|%S izvan zadane), cached_log(scope), input_with puni scope i kartu iz profila -- Leonov rad u granama ulazi u brojke (S-032)"
```

---

### Task 50: Radna stabla — vodeće stablo, dnevnici iz svih, docs iz vodećeg (spec §2, S-033)

**Files:**
- Modify: `crates/sokratis-io/src/git.rs` (trait: `worktree_heads`, `commit_times`, `last_changes(rev, pathspecs)`; nov `WorktreeHead`; `GitCli` impl)
- Modify: `crates/sokratis-io/src/project.rs` (`Lead`, `worktrees_by_recency`, `diaries`, `docs_in`, `input_with`)
- Modify: `crates/sokratis-io/src/lib.rs` (izvoz `WorktreeHead`)
- Modify: `crates/sokratis-io/tests/common/mod.rs` (`add_worktree`, `commit_at`), `tests/git_cli.rs`, `tests/project.rs`, `tests/perf.rs`, `tests/last_changes.rs` (novi potpis)

**Interfaces:**
- Produces:
  ```rust
  /// Jedno radno stablo iz `git worktree list --porcelain`: putanja + PUNI SHA HEAD-a (grana može
  /// nedostajati — detached).
  #[derive(Debug, Clone, PartialEq, Eq)]
  pub struct WorktreeHead { pub path: PathBuf, pub head: String }
  fn worktree_heads(&self) -> Result<Vec<WorktreeHead>, IoError>;
  /// `puni sha → author_time` jednim `git log --no-walk=unsorted --format=%H|%at <sha…>`; prazan popis → prazna mapa bez procesa.
  fn commit_times(&self, shas: &[String]) -> Result<HashMap<String, i64>, IoError>;
  fn last_changes(&self, rev: &str, pathspecs: &[&str]) -> Result<HashMap<String, i64>, IoError>;  // rev: "HEAD" ili SHA vodećeg stabla
  // project.rs
  pub struct Lead { pub root: PathBuf, pub head: String }
  pub fn lead(&self) -> Result<Lead, IoError>;   // stablo s najnovijim author_time HEAD-a; jedno stablo → root/"HEAD"
  pub fn docs(&self) -> Result<Vec<DocFile>, IoError>;  // = docs_in(&self.root, "HEAD") — nepromijenjen javni potpis
  ```
  `ReportInput.diaries` = dnevnici redom od vodećeg; `worktrees` = broj stabala; `plan`/`docs` iz vodećeg.

- [ ] **Step 1: Pomoćnici u `tests/common/mod.rs`**

```rust
    /// Drugo radno stablo na novoj grani `branch` u vlastitoj privremenoj mapi (RAII kao `dir`).
    /// `#[allow(dead_code)]`: koristi ga samo `project.rs`/`git_cli.rs`.
    #[allow(dead_code)]
    pub fn add_worktree(&self, branch: &str) -> TempDir {
        let dir = tempfile::tempdir().expect("tempdir");
        let wt = dir.path().join("wt");
        self.git(&["worktree", "add", "-q", "-b", branch, &wt.to_string_lossy()]);
        dir
    }

    /// Kao `commit`, ali u zadanoj mapi (drugo radno stablo). `commit` postaje `commit_at(self.path(), …)`.
    #[allow(dead_code)]
    pub fn commit_at(&self, dir: &Path, path: &str, content: &str, msg: &str, a: &str, c: &str) -> String {
        let full: PathBuf = dir.join(path);
        std::fs::create_dir_all(full.parent().expect("parent")).expect("mkdir");
        std::fs::write(&full, content).expect("write");
        let run = |args: &[&str]| {
            let out = Command::new("git")
                .arg("-C")
                .arg(dir)
                .env("GIT_AUTHOR_DATE", a)
                .env("GIT_COMMITTER_DATE", c)
                .args(args)
                .output()
                .expect("git");
            assert!(out.status.success(), "{}", String::from_utf8_lossy(&out.stderr));
            String::from_utf8_lossy(&out.stdout).trim().to_string()
        };
        run(&["add", "-A"]);
        run(&["commit", "-q", "-m", msg]);
        run(&["rev-parse", "--short", "HEAD"])
    }
```

(Stablo se koristi s `dir.path().join("wt")`; `TempDir` se vraća pozivatelju da živi do kraja testa.)

- [ ] **Step 2: Padajući testovi**

`tests/git_cli.rs`:

```rust
/// M2/50 (S-033): porcelain daje putanju + puni HEAD po stablu; detached stablo nema redak `branch`
/// i ne smije srušiti parser (Review Focus #2); `commit_times` vraća author_time po punom SHA-u.
#[test]
fn worktree_heads_parses_detached_worktree_and_commit_times_map_full_shas() {
    let r = Repo::init();
    r.commit("a.txt", "1", "prvi", "2026-09-10T10:00:00+02:00", "2026-09-10T10:00:00+02:00");
    let wt = r.add_worktree("feat/x");
    let detached = tempfile::tempdir().unwrap();
    let dpath = detached.path().join("d");
    r.git(&["worktree", "add", "-q", "--detach", &dpath.to_string_lossy()]);
    let g = GitCli::new(r.path());
    let heads = g.worktree_heads().unwrap();
    assert_eq!(heads.len(), 3, "glavno + feat/x + detached");
    assert!(heads.iter().all(|h| h.head.len() == 40), "puni SHA");
    let shas: Vec<String> = heads.iter().map(|h| h.head.clone()).collect();
    let times = g.commit_times(&shas).unwrap();
    assert_eq!(times.len(), 1, "sva tri HEAD-a su isti commit → jedan ključ");
    assert_eq!(times[&shas[0]], 1789027200, "author_time 2026-09-10T10:00:00+02:00 = 1789027200");
    assert!(g.commit_times(&[]).unwrap().is_empty());
    drop(wt);
}
```

(Očekivani `author_time`: `date -u -d "2026-09-10T10:00:00+02:00" +%s` = **1789027200** — graditelj to provjeri naredbom prije nego upiše.)

`tests/project.rs`:

```rust
/// M2/50 (S-033): dnevnik se čita iz SVAKOG stabla (unija; vodeće prvo → njegov `model` pobjeđuje),
/// plan i docs iz VODEĆEG (najnoviji HEAD); `touched.worktrees` = broj stabala.
#[test]
fn input_unites_diaries_and_reads_docs_from_lead_worktree() {
    let r = Repo::init();
    r.commit(
        "docs/records/PROGRESS.md",
        "## 2026-09-10 (OPUS) — F1/1 prvi\n",
        "docs: dnevnik",
        "2026-09-10T10:00:00+02:00",
        "2026-09-10T10:00:00+02:00",
    );
    let wt = r.add_worktree("feat/x");
    let wt_root = wt.path().join("wt");
    r.commit_at(
        &wt_root,
        "docs/records/PROGRESS.md",
        "## 2026-09-10 (FABLE) — F1/1 prvi\n\n## 2026-09-12 (FABLE) — F1/2 u grani\n",
        "docs: dnevnik u grani",
        "2026-09-12T10:00:00+02:00",
        "2026-09-12T10:00:00+02:00",
    );
    r.commit_at(&wt_root, "docs/plan/RASPORED.md", "### F1 · Prva\n| **F1/1** ✅ |\n", "plan u grani",
        "2026-09-12T11:00:00+02:00", "2026-09-12T11:00:00+02:00");

    let p = Project::open(r.path()).unwrap();
    let lead = p.lead().unwrap();
    assert_eq!(lead.root, wt_root, "stablo s najnovijim HEAD-om vodi");
    let input = p.input(Some("2026-09-01")).unwrap();
    assert_eq!(input.worktrees, 2);
    assert_eq!(input.diaries.len(), 2);
    assert!(input.diaries[0].contains("F1/2 u grani"), "vodeće stablo je PRVO");
    assert!(input.plan.as_deref().unwrap_or("").contains("F1/1"), "plan iz vodećeg stabla");
    assert!(input.docs.iter().any(|d| d.path == "docs/plan/RASPORED.md"), "docs iz vodećeg stabla");
    let r = sokratis_core::build_report(&input, &p.profile).unwrap();
    assert_eq!(r.deliveries.len(), 2, "unija: preklop jednom");
    assert_eq!(r.deliveries[0].model, "FABLE", "vodeće stablo pobjeđuje");
}

/// Review Focus #5: dnevnik postoji SAMO u sporednom stablu.
#[test]
fn diary_only_in_secondary_worktree_still_counts() {
    let r = Repo::init();
    r.commit("js/a.js", "1", "F1/1 kod", "2026-09-10T10:00:00+02:00", "2026-09-10T10:00:00+02:00");
    let wt = r.add_worktree("feat/x");
    let wt_root = wt.path().join("wt");
    r.commit_at(&wt_root, "docs/records/PROGRESS.md", "## 2026-09-12 (FABLE) — F1/2 u grani\n",
        "docs", "2026-09-12T10:00:00+02:00", "2026-09-12T10:00:00+02:00");
    let p = Project::open(r.path()).unwrap();
    let input = p.input(Some("2026-09-01")).unwrap();
    assert_eq!(input.diaries.len(), 1);
    let r = sokratis_core::build_report(&input, &p.profile).unwrap();
    assert_eq!(r.deliveries.len(), 1);
    assert_eq!(r.touched.diaries, 1);
}

/// Review Focus #1: jedno stablo → vodeće je `root`, HEAD.
#[test]
fn lead_is_root_when_there_is_one_worktree() {
    let r = Repo::init();
    r.commit("a.txt", "1", "prvi", "2026-09-10T10:00:00+02:00", "2026-09-10T10:00:00+02:00");
    let p = Project::open(r.path()).unwrap();
    let lead = p.lead().unwrap();
    assert_eq!(lead.root, p.root);
    assert_eq!(p.input(None).unwrap().worktrees, 1);
}
```

Run: `cargo test -p sokratis-io` → FAIL (nema `worktree_heads`, `lead`, `commit_at`).

- [ ] **Step 3: `GitCli`**

```rust
    fn worktree_heads(&self) -> Result<Vec<WorktreeHead>, IoError> {
        // Porcelain: blokovi `worktree <put>` / `HEAD <sha>` / (`branch …` | `detached`), prazan
        // redak između. Detached stablo NEMA redak `branch` — parser gleda samo prva dva.
        let out = self.run(&["worktree", "list", "--porcelain"])?;
        let mut heads = Vec::new();
        let mut path: Option<PathBuf> = None;
        for line in out.lines() {
            if let Some(p) = line.strip_prefix("worktree ") {
                path = Some(PathBuf::from(p.trim()));
            } else if let Some(sha) = line.strip_prefix("HEAD ")
                && let Some(p) = path.take()
            {
                heads.push(WorktreeHead { path: p, head: sha.trim().to_string() });
            }
        }
        Ok(heads)
    }
    fn commit_times(&self, shas: &[String]) -> Result<HashMap<String, i64>, IoError> {
        if shas.is_empty() {
            return Ok(HashMap::new());
        }
        let mut args = vec!["log", "--no-walk=unsorted", "--format=%H|%at"];
        let owned: Vec<&str> = shas.iter().map(String::as_str).collect();
        args.extend(owned);
        args.push("--");
        Ok(self
            .run(&args)?
            .lines()
            .filter_map(|l| l.split_once('|'))
            .filter_map(|(sha, at)| at.trim().parse::<i64>().ok().map(|t| (sha.trim().to_string(), t)))
            .collect())
    }
```

`last_changes(&self, rev: &str, pathspecs: &[&str])`: `args = vec!["-c", "core.quotepath=false", "log", rev, "--format=@@%at", "--diff-merges=combined", "--name-only", "--"]`. `worktrees()` ostaje (registar). `WorktreeHead` iznad traita, izvezen u `lib.rs`. Zaglavlje: odlomak M2/50 (let-chain `else if let … && let …`, `Option::take` u parseru, `--no-walk=unsorted`).

- [ ] **Step 4: `Project` — vodeće stablo, dnevnici, docs**

```rust
/// Vodeće stablo (S-033): ono čiji HEAD ima najnoviji `author_time` — tamo se radi, pa se odatle
/// čitaju plan i `docs/`. `head` je puni SHA: `last_changes` ga uzima kao reviziju, pa i detached
/// stablo (bez grane) ima povijest.
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct Lead {
    pub root: PathBuf,
    pub head: String,
}

impl Project {
    /// Sva radna stabla, od najnovijeg HEAD-a prema starijima (isti `author_time` → redoslijed
    /// gita, glavno stablo prvo). Dva procesa: `worktree list` + `commit_times`.
    fn worktrees_by_recency(&self) -> Result<Vec<WorktreeHead>, IoError> {
        let mut heads = self.git.worktree_heads()?;
        let shas: Vec<String> = heads.iter().map(|h| h.head.clone()).collect();
        let times = self.git.commit_times(&shas)?;
        // `sort_by_key` je stabilan; `Reverse` okreće u silazno bez ručnog komparatora.
        heads.sort_by_key(|h| std::cmp::Reverse(times.get(&h.head).copied().unwrap_or(0)));
        Ok(heads)
    }

    pub fn lead(&self) -> Result<Lead, IoError> {
        Ok(self
            .worktrees_by_recency()?
            .into_iter()
            .next()
            .map(|h| Lead { root: normalized(h.path), head: h.head })
            .unwrap_or_else(|| Lead { root: self.root.clone(), head: "HEAD".to_string() }))
    }

    /// Dnevnici s DISKA svakog stabla, zadanim redom (vodeće prvo) — necommitani unos je vidljiv.
    fn diaries(&self, ordered: &[WorktreeHead]) -> Vec<String> {
        ordered
            .iter()
            .filter_map(|h| std::fs::read_to_string(h.path.join(&self.profile.diary_path)).ok())
            .collect()
    }

    pub fn docs(&self) -> Result<Vec<DocFile>, IoError> {
        self.docs_in(&self.root, "HEAD")
    }

    /// Tijelo dosadašnjeg `docs()`, s korijenom i revizijom kao parametrima (vodeće stablo).
    fn docs_in(&self, root: &Path, rev: &str) -> Result<Vec<DocFile>, IoError> {
        /* isti kod kao prije, `self.root` → `root`, `last_changes(rev, &[…])` */
    }
}
```

`input_with`: `let ordered = self.worktrees_by_recency()?; let lead = ordered.first().map(|h| Lead { root: normalized(h.path.clone()), head: h.head.clone() }).unwrap_or_else(|| Lead { root: self.root.clone(), head: "HEAD".into() });` · `let read_lead = |rel: &str| std::fs::read_to_string(lead.root.join(rel)).ok();` · `diaries: self.diaries(&ordered)`, `plan: read_lead(&self.profile.plan_path)`, `docs: self.docs_in(&lead.root, &lead.head)?`, `worktrees: ordered.len().max(1) as u32`. (Ukloni privremeni `read_opt` za dnevnik iz T47; `read_opt` ostaje ako ga netko drugi koristi, inače briši.) `lead()` i `worktrees_by_recency` dijele kod — `lead` NE smije ponovno pokretati procese unutar `input_with` (zove se `ordered` jednom).

- [ ] **Step 5: `perf.rs` + `last_changes.rs`**

`tests/last_changes.rs`: pozivi `last_changes(&[…])` → `last_changes("HEAD", &[…])`. `tests/perf.rs`: granica **8** (`+1 commit_sources`, `+2 worktree list/commit_times`), ime `…at_most_eight_git_processes`; poruka navodi razdiobu.

- [ ] **Step 6: Brane + commit**

`cargo test --workspace` · clippy · fmt.

```bash
git add crates/sokratis-io
git commit -m "M2/50: worktree_heads + commit_times, Lead (vodece stablo po najnovijem HEAD-u), dnevnici iz svih stabala redom od vodeceg, plan/docs/last_changes iz vodeceg -- Isporuke i docs-ocjena prestaju stajati na 13. 9. (S-033)"
```

---

### Task 51: CLI `--scope` i redak opsega/grana u tablici (spec §1.3)

**Files:**
- Modify: `crates/sokratis-cli/src/main.rs` (`Report { … scope }`, `report_for(…, scope)`), `crates/sokratis-cli/src/table.rs` (`render`: redak „opseg", sekcija „grane"), `crates/sokratis-cli/tests/cli.rs`

**Interfaces:**
- Consumes: `Profile.branch_scope`, `Report.scope`, `Report.branches`.
- Produces: `sokratis report <put> [--scope all|default]`; tablica: redak `opseg: sve lokalne grane` / `opseg: samo zadana grana (main)` i tablica grana (`grana · commiti · sati · redci · spojena`).

- [ ] **Step 1: Padajući CLI testovi (`tests/cli.rs`)**

```rust
/// M2/51 (S-032): zadano se broje sve grane; `--scope default` vraća ponašanje 1.0.0-pre
/// (Review Focus #6). Repo: 1 commit na `main`, 1 u `feat/x`.
fn repo_with_feature_branch() -> Repo {
    let r = Repo::init();
    r.commit("js/a.js", "1", "F1/1 main", "2026-09-10T10:00:00+02:00", "2026-09-10T10:00:00+02:00");
    r.git(&["checkout", "-q", "-b", "feat/x"]);
    r.commit("js/b.js", "2", "F1/2 grana", "2026-09-11T10:00:00+02:00", "2026-09-11T10:00:00+02:00");
    r.git(&["checkout", "-q", "main"]);
    r
}

#[test]
fn report_counts_all_branches_by_default_and_labels_rows() {
    let r = repo_with_feature_branch();
    let out = bin().args(["report", "--json", "--since", "2026-09-01"]).arg(r.path()).output().unwrap();
    assert!(out.status.success());
    let v: serde_json::Value = serde_json::from_slice(&out.stdout).unwrap();
    assert_eq!(v["scope"], "all");
    assert_eq!(v["touched"]["commits"], 2);
    assert_eq!(v["branches"].as_array().unwrap().len(), 2);
    assert!(v["commits"].as_array().unwrap().iter().any(|c| c["branch"] == "feat/x"));
}

#[test]
fn scope_default_matches_pre_1_0_behaviour() {
    let r = repo_with_feature_branch();
    let out = bin().args(["report", "--json", "--since", "2026-09-01", "--scope", "default"]).arg(r.path()).output().unwrap();
    let v: serde_json::Value = serde_json::from_slice(&out.stdout).unwrap();
    assert_eq!(v["scope"], "default");
    assert_eq!(v["touched"]["commits"], 1, "samo main, kao 1.0.0-pre");
    assert_eq!(v["branches"].as_array().unwrap().len(), 1);
    assert_eq!(v["days"].as_array().unwrap().len(), 1);
}

#[test]
fn scope_rejects_unknown_value_as_usage_error() {
    let r = repo_with_feature_branch();
    let out = bin().args(["report", "--scope", "worktrees"]).arg(r.path()).output().unwrap();
    assert_eq!(out.status.code(), Some(3), "pogrešna uporaba = 3, ne 2 (Alert)");
}

#[test]
fn table_shows_scope_and_branches() {
    let r = repo_with_feature_branch();
    let out = bin().args(["report", "--table", "--since", "2026-09-01"]).arg(r.path()).output().unwrap();
    let s = String::from_utf8_lossy(&out.stdout);
    assert!(s.contains("opseg"), "{s}");
    assert!(s.contains("feat/x"), "{s}");
}
```

(`serde_json` je dev-ovisnost CLI-ja — provjeri `Cargo.toml`; ako nije, koristi `cli` postojeći obrazac čitanja JSON-a iz `report_until_limits_the_window…`.)

Run: `cargo test -p sokratis-cli` → FAIL (`--scope` nepoznat).

- [ ] **Step 2: `main.rs`**

U `Cmd::Report` iza `until`:

```rust
        /// Koje grane ulaze u brojke (S-032): `all` = sve lokalne grane (zadano iz profila), `default`
        /// = samo zadana grana (paritet s RAD.xlsx). Pregazi `branch_scope` iz profila.
        #[arg(long, value_parser = ["all", "default"])]
        scope: Option<String>,
```

`report_for(path, since, until, scope: Option<&str>)`:

```rust
    let mut project = Project::open(&path)?;
    match scope {
        Some("all") => project.profile.branch_scope = BranchScope::AllBranches,
        Some("default") => project.profile.branch_scope = BranchScope::DefaultBranch,
        _ => {}
    }
```

(`use sokratis_core::BranchScope`.) `Docs`/`Signals` zovu `report_for(path, None, None, None)`. Zaglavlje: 2 retka (`value_parser = [..]` daje grešku uporabe → 3; profil se mijenja u memoriji, datoteka se ne dira).

- [ ] **Step 3: `table.rs`**

U `render`, iza retka s granom: `opseg: {}` gdje je tekst `"sve lokalne grane"` za `AllBranches`, `"samo zadana grana"` za `DefaultBranch`; nova sekcija iza `kinds`:

```
grane
  grana      commiti   sati   redci   spojena
  main             1    0,5       1   da
  feat/x           1    0,5       2   ne
```

(isti stil kao postojeće tablice; `label()` ne treba nov id — nazivi grana su podaci). Postojeći `table.rs` unit-testovi grade `Report` sa `scope`/`branches` (T46) — dopuni jedan da tvrdi `opseg` u izlazu.

- [ ] **Step 4: Brane + commit**

`cargo test --workspace` · clippy · fmt.

```bash
git add crates/sokratis-cli
git commit -m "M2/51: report --scope all|default (pregazi profil), tablica s retkom opsega i granama -- svaku brojku s ploce Leon moze ponoviti iz terminala (S-012, S-032)"
```

---

### Task 52: Mjerenje nad Sokrat Studyjem (samo čitanje) — brojke, procesi, trajanje

**Files:**
- Modify: `crates/sokratis-io/tests/perf.rs` (nov `#[ignore]` test nad putanjom iz env-varijable)
- Create: `.superpowers/sdd/2026-09-18-m2-desktop/task-52-report.md` (izvještaj cigle; čuvar iz njega puni `CHANGELOG.md`, S-010)

**Interfaces:** ništa novo u kodu; `SOKRATIS_MEASURE_REPO` (env) → putanja repoa za mjerenje.

- [ ] **Step 1: Test koji se pokreće ručno**

```rust
/// M2/52 — mjerenje nad PRAVIM repoom (Sokrat Study), samo čitanje: broj git-procesa i trajanje
/// `input()` za oba opsega. `#[ignore]` jer ovisi o stroju; pokreće se ručno:
/// `SOKRATIS_MEASURE_REPO=C:/Users/leonk/Documents/sokratstudy.dev cargo test -p sokratis-io --test perf -- --ignored --nocapture`
#[test]
#[ignore]
fn measure_real_repo_both_scopes() {
    let Some(path) = std::env::var_os("SOKRATIS_MEASURE_REPO") else {
        eprintln!("SOKRATIS_MEASURE_REPO nije zadan — preskačem");
        return;
    };
    for scope in ["all", "default"] {
        let mut p = Project::open(std::path::Path::new(&path)).unwrap();
        p.profile.branch_scope = if scope == "all" {
            sokratis_core::BranchScope::AllBranches
        } else {
            sokratis_core::BranchScope::DefaultBranch
        };
        let before = p.git.calls();
        let t = Instant::now();
        let input = p.input(None).unwrap();
        let elapsed = t.elapsed();
        let r = sokratis_core::build_report(&input, &p.profile).unwrap();
        eprintln!(
            "scope={scope}: {} git procesa, {:?}, commita {}, grana {}, isporuka {}, stabala {}, dnevnika {}",
            p.git.calls() - before, elapsed, r.touched.commits, r.branches.len(), r.deliveries.len(),
            r.touched.worktrees, r.touched.diaries
        );
        assert!(p.git.calls() - before <= 8);
    }
}
```

- [ ] **Step 2: Pokreni i zapiši**

Run (Bash, `export PATH="/c/Users/leonk/.cargo/bin:$PATH"`): naredba iz doc-komentara; zatim `cargo run -p sokratis-cli -- report C:/Users/leonk/Documents/sokratstudy.dev --json > $SCRATCH/ss-all.json` i `… --scope default --json > $SCRATCH/ss-default.json`; u izvještaj: `touched.commits` (očekivano: `all` ≥ 190 + ≈ 95 commita iz grana od 13. 9.), `branches` (imena i commiti — `feat/f6-mcp` ≈ 90), `deliveries` (all vs default), `touched.worktrees` (= 6), `touched.diaries`, zadnji dan u `days` (mora biti **2026-09-23**, ne 13. 9.), broj procesa (≤ 8), trajanje oba opsega. Provjeri `git -C C:/Users/leonk/Documents/sokratstudy.dev status --porcelain` PRIJE i POSLIJE — nepromijenjeno (samo Leonova `RAD.xlsx` ako je već bila).

- [ ] **Step 3: Commit**

```bash
git add crates/sokratis-io/tests/perf.rs
git commit -m "M2/52: rucno mjerenje nad pravim repoom (SOKRATIS_MEASURE_REPO, #[ignore]) -- brojke u task-52-report.md, granica 8 procesa potvrdjena nad Sokrat Studyjem (pravilo #4)"
```

**Orkestrator nakon spajanja IO-2:** čuvar A — `ARCHITECTURE.md` §1 (`io` radi: `Scope`, `commit_sources`, `worktree_heads`, `Lead`, docs iz vodećeg; broj procesa iz `task-52-report.md`), §2 tok podataka („Grana" odlomak prepisan: zadano sve lokalne grane), CLI naredbe (`--scope`); `CLAUDE.md` komande; `CHANGELOG.md` brojke iz T52. **Instalater „1.0.0-pre.3"** na `main`-u (sesija 3) — Leon vidi svoj rad.

---
## Tok GRAFOVI — d3-matematika + naš SVG (spec §3.3, S-035)

Sve u `apps/desktop`; brane `npm run check` + `npm run build`. Nema DOM-a u vitestu: svaki graf = **čista funkcija rasporeda** (`layout.ts`, testirana) + tanka komponenta. Boje SAMO `var(--color-*)`.

### Task 53: Ovisnosti d3 (Leonov OK, S-035), `scales.ts`, `bucket.ts`

**Files:**
- Modify: `apps/desktop/package.json` (blok `dependencies` += 4; `devDependencies` += 4 `@types/*`), `apps/desktop/package-lock.json`
- Create: `apps/desktop/src/lib/charts/scales.ts`, `apps/desktop/src/lib/charts/bucket.ts`
- Test: `apps/desktop/tests/scales.test.ts`, `apps/desktop/tests/bucket.test.ts`

**Interfaces:**
- Produces (`scales.ts`):
  ```ts
  export function parseYmd(ymd: string): Date;                       // UTC ponoć
  export function toYmd(d: Date): string;                            // 'YYYY-MM-DD' iz UTC polja
  export function timeScale(domain: [Date, Date], range: [number, number]): ScaleTime<number, number>; // scaleUtc
  export function dateTicks(domain: [Date, Date], widthPx: number, lang: Lang): { at: Date; x: number; label: string }[];
  export function yTicks(max: number, count?: number): number[];     // scaleLinear().domain([0,max]).nice().ticks(count)
  export function niceMax(max: number): number;                      // gornja granica nice domene, ≥ 1
  export function formatDate(ymd: string, lang: Lang, g: 'day' | 'week' | 'month'): string;  // '10.09.' | 'tj. 37' | 'ruj 2026'
  ```
- Produces (`bucket.ts`):
  ```ts
  export type Granularity = 'day' | 'week' | 'month';
  export interface Bucket { start: string; commits: number; hours: number; lines: number; deliveries: number; deploys: number; test_lines: number }
  export function bucketStart(ymd: string, g: Granularity): string;  // day → isti; week → ISO ponedjeljak; month → 'YYYY-MM-01'
  export function bucketDays(days: DayStats[], g: Granularity): Bucket[];           // zbroj po bucketStart, uzlazno
  export function bucketKinds(rows: CommitRow[], g: Granularity): { start: string; counts: Record<WorkKind, number> }[];
  export function bucketDeliveries(rows: Delivery[], g: Granularity): { start: string; count: number; deploys: number }[];
  export function hourOfDay(authorTime: number): number;              // lokalna zona OVOG računala (spec §3.2)
  export function hourHistogram(rows: CommitRow[]): number[];        // 24 binova
  ```

- [ ] **Step 1: Ovisnosti (OK dan 2026-09-24)**

Run u `apps/desktop`: `npm install --save-exact d3-scale@4.0.2 d3-shape@3.2.0 d3-array@3.2.4 d3-time-format@4.1.0` · `npm install --save-exact --save-dev @types/d3-scale@4.0.9 @types/d3-shape@3.1.7 @types/d3-array@3.2.2 @types/d3-time-format@4.0.3` (verzije `@types/*`: `npm view <paket> version` u trenutku izvedbe — upiši STVARNE u izvještaj; ako se razlikuju od ovih, prevladava npm). Provjeri `git diff package.json` = točno 8 redaka bez `^`; `npm ci` nakon toga ne mijenja lock (`git status` čist). **Bez izravnog uvoza `d3-time`** (tranzitivan, nije pinan): datumski ticksi idu kroz `scaleUtc().ticks()`.

- [ ] **Step 2: Padajući testovi — `tests/bucket.test.ts`**

```ts
// ZAŠTO OVAKO (cigla M2/53 — dan/tjedan/mjesec je preslagivanje već izmjerenih dana, ne mjerenje;
// dopuna S-012). Sve funkcije su čiste; rubovi: prazan ulaz, prijelaz godine, ISO tjedan koji
// počinje u prošloj godini, sat u lokalnoj zoni.
import { describe, expect, it } from 'vitest';
import { bucketDays, bucketDeliveries, bucketKinds, bucketStart, hourHistogram, hourOfDay } from '../src/lib/charts/bucket';
import type { CommitRow, DayStats, Delivery } from '../src/lib/types';

const day = (date: string, commits: number, hours = 1): DayStats => ({
  date, commits, commits_cumulative: 0, lines: 10, hours, deliveries: 0, deploys: 0, test_lines: 0,
});

describe('bucketStart', () => {
  it('dan je sam sebi početak', () => expect(bucketStart('2026-09-10', 'day')).toBe('2026-09-10'));
  it('tjedan počinje ISO ponedjeljkom, i preko granice godine', () => {
    expect(bucketStart('2026-09-10', 'week')).toBe('2026-09-07'); // četvrtak → ponedjeljak
    expect(bucketStart('2026-09-07', 'week')).toBe('2026-09-07');
    expect(bucketStart('2026-09-13', 'week')).toBe('2026-09-07'); // nedjelja → isti tjedan
    expect(bucketStart('2027-01-01', 'week')).toBe('2026-12-28'); // petak → ponedjeljak prošle godine
  });
  it('mjesec počinje prvim', () => expect(bucketStart('2026-09-10', 'month')).toBe('2026-09-01'));
});

describe('bucketDays', () => {
  it('vraća [] za prazan ulaz', () => expect(bucketDays([], 'week')).toEqual([]));
  it('zbraja dane u tjedne uzlazno', () => {
    const out = bucketDays([day('2026-09-13', 2, 1.5), day('2026-09-07', 1, 0.5), day('2026-09-14', 3)], 'week');
    expect(out.map((b) => [b.start, b.commits, b.hours])).toEqual([
      ['2026-09-07', 3, 2],
      ['2026-09-14', 3, 1],
    ]);
  });
  it('dan = identitet po datumu', () => {
    expect(bucketDays([day('2026-09-10', 2)], 'day')[0]).toMatchObject({ start: '2026-09-10', commits: 2 });
  });
});

describe('bucketKinds / bucketDeliveries', () => {
  const row = (date: string, kind: CommitRow['kind'], author_time = 0): CommitRow => ({
    sha: date + kind, date, author_time, subject: '', kind, sub: 'other', overridden: false, branch: 'main',
  });
  it('broji vrste po tjednu sa svih pet ključeva', () => {
    const out = bucketKinds([row('2026-09-10', 'execution'), row('2026-09-11', 'debugging'), row('2026-09-15', 'execution')], 'week');
    expect(out).toHaveLength(2);
    expect(out[0].counts).toEqual({ planning: 0, documentation: 0, execution: 1, polish: 0, debugging: 1 });
  });
  it('broji isporuke i deploye po tjednu', () => {
    const d = (date: string, deploy: boolean): Delivery => ({ date, model: '', title: 't', kind: 'execution', deploy });
    expect(bucketDeliveries([d('2026-09-10', true), d('2026-09-11', false)], 'week')).toEqual([
      { start: '2026-09-07', count: 2, deploys: 1 },
    ]);
  });
});

describe('doba dana', () => {
  it('sat je u lokalnoj zoni računala i histogram ima 24 bina', () => {
    const t = Date.UTC(2026, 8, 10, 12, 0, 0) / 1000; // podne UTC
    expect(hourOfDay(t)).toBe(new Date(t * 1000).getHours());
    const h = hourHistogram([{ sha: 'a', date: '2026-09-10', author_time: t, subject: '', kind: 'execution', sub: 'other', overridden: false, branch: 'main' }]);
    expect(h).toHaveLength(24);
    expect(h.reduce((a, b) => a + b, 0)).toBe(1);
  });
});
```

`tests/scales.test.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { dateTicks, formatDate, niceMax, parseYmd, toYmd, yTicks } from '../src/lib/charts/scales';

describe('scales', () => {
  it('parseYmd/toYmd su inverzi u UTC-u', () => {
    expect(toYmd(parseYmd('2026-09-10'))).toBe('2026-09-10');
    expect(parseYmd('2026-09-10').getUTCHours()).toBe(0);
  });
  it('7 dana → dnevni ticksi s HR datumima, 6 mjeseci → mjesečni', () => {
    const week = dateTicks([parseYmd('2026-09-07'), parseYmd('2026-09-13')], 600, 'hr');
    expect(week.length).toBeGreaterThanOrEqual(4);
    expect(week[0].label).toMatch(/^\d{2}\.\d{2}\.$/); // '07.09.'
    const half = dateTicks([parseYmd('2026-03-01'), parseYmd('2026-09-01')], 600, 'hr');
    expect(half.some((t) => /ožu|tra|svi|lip|srp|kol|ruj/.test(t.label))).toBe(true);
    const en = dateTicks([parseYmd('2026-03-01'), parseYmd('2026-09-01')], 600, 'en');
    expect(en.some((t) => /Mar|Apr|May|Jun|Jul|Aug|Sep/.test(t.label))).toBe(true);
  });
  it('ticksi su unutar raspona i monotoni', () => {
    const t = dateTicks([parseYmd('2026-08-29'), parseYmd('2026-09-23')], 600, 'en');
    for (let i = 1; i < t.length; i++) expect(t[i].x).toBeGreaterThan(t[i - 1].x);
    expect(t[0].x).toBeGreaterThanOrEqual(0);
    expect(t.at(-1)!.x).toBeLessThanOrEqual(600);
  });
  it('yTicks i niceMax bez NaN i za nulu', () => {
    expect(yTicks(0)).toEqual([0, 1]);
    expect(niceMax(0)).toBe(1);
    expect(niceMax(Number.NaN)).toBe(1);
    expect(yTicks(37)).toEqual([0, 10, 20, 30, 40]);
  });
  it('formatDate po zrnatosti', () => {
    expect(formatDate('2026-09-10', 'hr', 'day')).toBe('10.09.');
    expect(formatDate('2026-09-07', 'hr', 'week')).toBe('tj. 37');
    expect(formatDate('2026-09-01', 'hr', 'month')).toBe('ruj 2026');
    expect(formatDate('2026-09-01', 'en', 'month')).toBe('Sep 2026');
  });
});
```

Run: `npm run test` → FAIL (moduli ne postoje).

- [ ] **Step 3: `scales.ts`**

```ts
// ZAŠTO OVAKO (cigla M2/53 — d3-matematika bez DOM-a, S-035)
// `d3-scale` daje `nice()`, `ticks()` i datumske ticksove koji znaju kad prijeći s dana na tjedne i
// mjesece — to je dio koji smo do sada pisali sami i zato nismo imali datume na osi. `scaleUtc` (ne
// `scaleTime`): datumi u `Report` su civilni (`YYYY-MM-DD`), bez zone; UTC ponoć drži ticksove na
// istom mjestu bez obzira na ljetno vrijeme računala. `timeFormatLocale` nosi HR nazive mjeseci i
// dana; `d3-time` se NE uvozi izravno (tranzitivan, nije pinan) — ticksove daje sama skala.
import { scaleLinear, scaleUtc, type ScaleTime } from 'd3-scale';
import { timeFormatLocale, type TimeLocaleDefinition } from 'd3-time-format';
import type { Lang } from '../types';

const HR: TimeLocaleDefinition = {
  dateTime: '%A, %e. %B %Y. %X', date: '%d.%m.%Y.', time: '%H:%M:%S', periods: ['AM', 'PM'],
  days: ['nedjelja', 'ponedjeljak', 'utorak', 'srijeda', 'četvrtak', 'petak', 'subota'],
  shortDays: ['ned', 'pon', 'uto', 'sri', 'čet', 'pet', 'sub'],
  months: ['siječanj', 'veljača', 'ožujak', 'travanj', 'svibanj', 'lipanj', 'srpanj', 'kolovoz', 'rujan', 'listopad', 'studeni', 'prosinac'],
  shortMonths: ['sij', 'velj', 'ožu', 'tra', 'svi', 'lip', 'srp', 'kol', 'ruj', 'lis', 'stu', 'pro'],
};
const EN: TimeLocaleDefinition = {
  dateTime: '%x, %X', date: '%m/%d/%Y', time: '%H:%M:%S', periods: ['AM', 'PM'],
  days: ['Sunday', 'Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday'],
  shortDays: ['Sun', 'Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat'],
  months: ['January', 'February', 'March', 'April', 'May', 'June', 'July', 'August', 'September', 'October', 'November', 'December'],
  shortMonths: ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun', 'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'],
};
const locale = (lang: Lang) => timeFormatLocale(lang === 'hr' ? HR : EN);

export function parseYmd(ymd: string): Date {
  const [y, m, d] = ymd.split('-').map(Number);
  return new Date(Date.UTC(y, (m ?? 1) - 1, d ?? 1));
}
export function toYmd(d: Date): string {
  return d.toISOString().slice(0, 10);
}
export function timeScale(domain: [Date, Date], range: [number, number]): ScaleTime<number, number> {
  return scaleUtc().domain(domain).range(range);
}
export function niceMax(max: number): number {
  if (!Number.isFinite(max) || max <= 0) return 1;
  return scaleLinear().domain([0, max]).nice().domain()[1];
}
export function yTicks(max: number, count = 4): number[] {
  return scaleLinear().domain([0, niceMax(max)]).ticks(count);
}
// Dan-razina kad je raspon kraći od ≈ 2 mjeseca, inače mjesec-razina; d3 sam bira gustoću po `count`.
export function dateTicks(domain: [Date, Date], widthPx: number, lang: Lang): { at: Date; x: number; label: string }[] {
  const scale = timeScale(domain, [0, widthPx]);
  const count = Math.max(2, Math.floor(widthPx / 80));
  const days = (domain[1].getTime() - domain[0].getTime()) / 86_400_000;
  const fmt = locale(lang).utcFormat(days <= 62 ? (lang === 'hr' ? '%d.%m.' : '%b %d') : '%b %Y');
  return scale.ticks(count).map((at) => ({ at, x: scale(at), label: fmt(at) }));
}
export function formatDate(ymd: string, lang: Lang, g: 'day' | 'week' | 'month'): string {
  const d = parseYmd(ymd);
  const l = locale(lang);
  if (g === 'day') return l.utcFormat(lang === 'hr' ? '%d.%m.' : '%b %d')(d);
  if (g === 'week') return (lang === 'hr' ? 'tj. ' : 'wk ') + l.utcFormat('%V')(d);
  return l.utcFormat('%b %Y')(d);
}
```

- [ ] **Step 4: `bucket.ts`**

```ts
// ZAŠTO OVAKO (cigla M2/53 — preslagivanje dana u tjedne/mjesece; dopuna S-012)
// Ulaz su VEĆ izmjereni `DayStats`/`CommitRow`/`Delivery` iz jezgre; ovdje se samo zbrajaju po ključu
// razdoblja. ISO tjedan računamo sami nad UTC datumom (ponedjeljak = početak) — čista aritmetika,
// bez `d3-time`. Sat u danu je u ZONI OVOG RAČUNALA (jezgra ne nosi sat autora) — kartica to kaže.
import { parseYmd, toYmd } from './scales';
import type { CommitRow, DayStats, Delivery, WorkKind } from '../types';

export type Granularity = 'day' | 'week' | 'month';
export interface Bucket { start: string; commits: number; hours: number; lines: number; deliveries: number; deploys: number; test_lines: number }

const KINDS: WorkKind[] = ['planning', 'documentation', 'execution', 'polish', 'debugging'];

export function bucketStart(ymd: string, g: Granularity): string {
  if (g === 'day') return ymd;
  const d = parseYmd(ymd);
  if (g === 'month') return ymd.slice(0, 7) + '-01';
  d.setUTCDate(d.getUTCDate() - ((d.getUTCDay() + 6) % 7));
  return toYmd(d);
}

function groupBy<T, R>(rows: T[], g: Granularity, date: (r: T) => string, empty: (start: string) => R, add: (acc: R, r: T) => void): R[] {
  const map = new Map<string, R>();
  for (const r of rows) {
    const start = bucketStart(date(r), g);
    let acc = map.get(start);
    if (!acc) { acc = empty(start); map.set(start, acc); }
    add(acc, r);
  }
  return [...map.values()].sort((a, b) => ((a as { start: string }).start < (b as { start: string }).start ? -1 : 1));
}

export function bucketDays(days: DayStats[], g: Granularity): Bucket[] {
  return groupBy(days, g, (d) => d.date,
    (start) => ({ start, commits: 0, hours: 0, lines: 0, deliveries: 0, deploys: 0, test_lines: 0 }),
    (b, d) => { b.commits += d.commits; b.hours += d.hours; b.lines += d.lines; b.deliveries += d.deliveries; b.deploys += d.deploys; b.test_lines += d.test_lines; });
}
export function bucketKinds(rows: CommitRow[], g: Granularity): { start: string; counts: Record<WorkKind, number> }[] {
  return groupBy(rows, g, (r) => r.date,
    (start) => ({ start, counts: Object.fromEntries(KINDS.map((k) => [k, 0])) as Record<WorkKind, number> }),
    (b, r) => { b.counts[r.kind] += 1; });
}
export function bucketDeliveries(rows: Delivery[], g: Granularity): { start: string; count: number; deploys: number }[] {
  return groupBy(rows, g, (r) => r.date, (start) => ({ start, count: 0, deploys: 0 }), (b, r) => { b.count += 1; if (r.deploy) b.deploys += 1; });
}
export function hourOfDay(authorTime: number): number {
  return new Date(authorTime * 1000).getHours();
}
export function hourHistogram(rows: CommitRow[]): number[] {
  const h = Array.from({ length: 24 }, () => 0);
  for (const r of rows) h[hourOfDay(r.author_time)] += 1;
  return h;
}
```

(`groupBy` s generičkim `R` — sort kroz `start`; ako TS prigovara na cast, dodaj ograničenje `R extends { start: string }`.)

- [ ] **Step 5: Brane + commit**

`npm run check` · `npm run build` (bundle raste za d3 module — zapiši `main.js` kB u izvještaj; očekivano < 60 kB gzip razlike).

```bash
git add package.json package-lock.json src/lib/charts/scales.ts src/lib/charts/bucket.ts tests/scales.test.ts tests/bucket.test.ts
git commit -m "M2/53: d3-scale/shape/array/time-format (pinano, Leonov OK S-035), scales.ts (UTC datumski ticksi HR/EN), bucket.ts (dan/tjedan/mjesec, doba dana) -- matematika osi iz d3, crtanje ostaje nase"
```

---

### Task 54: Temelji — `layout.ts` (okvir, stupci, linija, najbliža točka), `Chart` · `Axis` · `Grid` · `Legend` · `Tooltip`

**Files:**
- Create: `apps/desktop/src/lib/charts/layout.ts`, `Chart.svelte`, `Axis.svelte`, `Grid.svelte`, `Legend.svelte`, `Tooltip.svelte`
- Modify: `apps/desktop/src/styles/motion.css` (klasa `chart-tip` — fade)
- Test: `apps/desktop/tests/layout.test.ts`

**Interfaces:**
```ts
// layout.ts
export interface Margins { top: number; right: number; bottom: number; left: number }
export interface Frame { width: number; height: number; m: Margins; innerW: number; innerH: number }
export function frame(width: number, height: number, m?: Partial<Margins>): Frame;   // zadano {top:12,right:12,bottom:28,left:40}
export interface XTick { x: number; label: string }
export interface YTick { y: number; label: string }
export interface Series { id: string; label: string; color: string }                 // color = 'var(--color-…)'
export interface Bar { x: number; y: number; w: number; h: number; series: string; value: number; start: string }
export interface BarsLayout { bars: Bar[]; xTicks: XTick[]; yTicks: YTick[]; baseline: number; centers: number[] }
export function barsLayout(f: Frame, buckets: { start: string; values: Record<string, number> }[], series: Series[], g: Granularity, lang: Lang, stacked?: boolean): BarsLayout;
export interface LinePoint { x: number; y: number; value: number; date: string }
export interface LineLayout { paths: { series: string; d: string; points: LinePoint[] }[]; xTicks: XTick[]; yTicks: YTick[] }
export function lineLayout(f: Frame, series: (Series & { points: { date: string; value: number }[] })[], lang: Lang): LineLayout;
export function nearestIndex(xs: number[], x: number): number;                       // -1 za prazno
export function linePath(pts: { x: number; y: number }[]): string;                   // preseljeno iz scale.ts (T55 briše izvor)
// Chart.svelte props: { width?: number = 600; height?: number = 200; label?: string; tip?: { x: number; y: number; lines: string[] } | null; children: Snippet<[Frame]> }
// Axis.svelte props:  { f: Frame; xTicks: XTick[]; yTicks: YTick[] }
// Grid.svelte props:  { f: Frame; yTicks: YTick[] }
// Legend.svelte props:{ items: { label: string; color: string }[] }
// Tooltip.svelte props: { width: number; height: number; tip: { x: number; y: number; lines: string[] } }   // HTML iznad SVG-a, položaj u %
```

- [ ] **Step 1: Padajući test — `tests/layout.test.ts`**

```ts
import { describe, expect, it } from 'vitest';
import { barsLayout, frame, lineLayout, nearestIndex } from '../src/lib/charts/layout';

const S = [{ id: 'commits', label: 'commiti', color: 'var(--color-brand-500)' }];

describe('frame', () => {
  it('unutarnje mjere = vanjske − margine', () => {
    const f = frame(600, 200);
    expect(f.innerW).toBe(600 - 40 - 12);
    expect(f.innerH).toBe(200 - 12 - 28);
  });
});

describe('barsLayout', () => {
  it('prazan ulaz → bez stupaca, bez NaN, baseline unutar okvira', () => {
    const l = barsLayout(frame(600, 200), [], S, 'day', 'hr');
    expect(l.bars).toEqual([]);
    expect(Number.isFinite(l.baseline)).toBe(true);
    expect(l.yTicks.length).toBeGreaterThan(0);
  });
  it('tri dana → tri stupca, x ticksi s datumima, visina proporcionalna', () => {
    const l = barsLayout(frame(600, 200), [
      { start: '2026-09-10', values: { commits: 2 } },
      { start: '2026-09-11', values: { commits: 4 } },
      { start: '2026-09-12', values: { commits: 0 } },
    ], S, 'day', 'hr');
    expect(l.bars).toHaveLength(3);
    expect(l.bars[1].h).toBeCloseTo(l.bars[0].h * 2, 5);
    expect(l.bars[2].h).toBe(0);
    expect(l.xTicks.map((t) => t.label)).toEqual(['10.09.', '11.09.', '12.09.']);
    for (const b of l.bars) expect([b.x, b.y, b.w, b.h].every(Number.isFinite)).toBe(true);
  });
  it('naslagano: drugi niz sjedi na prvom', () => {
    const two = [...S, { id: 'hours', label: 'sati', color: 'var(--color-accent)' }];
    const l = barsLayout(frame(600, 200), [{ start: '2026-09-10', values: { commits: 2, hours: 3 } }], two, 'day', 'hr', true);
    const [a, b] = l.bars;
    expect(b.y + b.h).toBeCloseTo(a.y, 5);
  });
  it('mnogo tjedana → ticksi prorijeđeni (≤ 10 natpisa)', () => {
    const buckets = Array.from({ length: 30 }, (_, i) => ({ start: `2026-${String(1 + Math.floor(i / 4)).padStart(2, '0')}-${String(1 + (i % 4) * 7).padStart(2, '0')}`, values: { commits: 1 } }));
    const l = barsLayout(frame(600, 200), buckets, S, 'week', 'hr');
    expect(l.xTicks.length).toBeLessThanOrEqual(10);
  });
});

describe('lineLayout', () => {
  it('dva niza → dvije putanje, ticksi s datumima, prazno → bez putanja', () => {
    const l = lineLayout(frame(600, 200), [
      { ...S[0], points: [{ date: '2026-09-10', value: 1 }, { date: '2026-09-20', value: 5 }] },
      { id: 'w', label: 'w', color: 'var(--color-warn)', points: [{ date: '2026-09-15', value: 2 }] },
    ], 'en');
    expect(l.paths).toHaveLength(2);
    expect(l.paths[0].d.startsWith('M')).toBe(true);
    expect(l.xTicks.length).toBeGreaterThan(1);
    expect(lineLayout(frame(600, 200), [], 'en').paths).toEqual([]);
  });
});

describe('nearestIndex', () => {
  it('najbliži x, −1 za prazno', () => {
    expect(nearestIndex([10, 20, 30], 22)).toBe(1);
    expect(nearestIndex([], 5)).toBe(-1);
  });
});
```

Run: `npm run test -- tests/layout.test.ts` → FAIL.

- [ ] **Step 2: `layout.ts`**

```ts
// ZAŠTO OVAKO (cigla M2/54 — raspored grafova kao čiste funkcije, S-035)
// Nema DOM-a u vitestu, pa SVE što se može pogrešno izračunati (koordinate, ticksovi, stack, najbliža
// točka) živi ovdje i ima test; Svelte komponente samo mapiraju ovaj izlaz u `<rect>`/`<path>`.
// `scaleBand` (d3-scale) dijeli širinu na jednake trake s razmakom — isti obrazac za dane, tjedne i
// mjesece. `stack` iz d3-shape nije potreban: naslagani stupci su jedno zbrajanje po nizu.
import { scaleBand } from 'd3-scale';
import { dateTicks, formatDate, niceMax, parseYmd, timeScale, yTicks } from './scales';
import type { Granularity } from './bucket';
import type { Lang } from '../types';

export interface Margins { top: number; right: number; bottom: number; left: number }
export interface Frame { width: number; height: number; m: Margins; innerW: number; innerH: number }
export interface XTick { x: number; label: string }
export interface YTick { y: number; label: string }
export interface Series { id: string; label: string; color: string }
export interface Bar { x: number; y: number; w: number; h: number; series: string; value: number; start: string }
export interface BarsLayout { bars: Bar[]; xTicks: XTick[]; yTicks: YTick[]; baseline: number; centers: number[] }
export interface LinePoint { x: number; y: number; value: number; date: string }
export interface LineLayout { paths: { series: string; d: string; points: LinePoint[] }[]; xTicks: XTick[]; yTicks: YTick[] }

const M: Margins = { top: 12, right: 12, bottom: 28, left: 40 };

export function frame(width: number, height: number, m: Partial<Margins> = {}): Frame {
  const mm = { ...M, ...m };
  return { width, height, m: mm, innerW: Math.max(0, width - mm.left - mm.right), innerH: Math.max(0, height - mm.top - mm.bottom) };
}

function yScale(f: Frame, max: number): (v: number) => number {
  const top = niceMax(max);
  return (v) => f.m.top + f.innerH - (v / top) * f.innerH;
}
function yTicksFor(f: Frame, max: number): YTick[] {
  const y = yScale(f, max);
  return yTicks(max).map((v) => ({ y: y(v), label: String(v) }));
}

export function barsLayout(f: Frame, buckets: { start: string; values: Record<string, number> }[], series: Series[], g: Granularity, lang: Lang, stacked = false): BarsLayout {
  const starts = buckets.map((b) => b.start);
  const x = scaleBand<string>().domain(starts).range([f.m.left, f.m.left + f.innerW]).paddingInner(0.2).paddingOuter(0.1);
  const totals = buckets.map((b) => stacked ? series.reduce((s, sr) => s + (b.values[sr.id] ?? 0), 0) : Math.max(0, ...series.map((sr) => b.values[sr.id] ?? 0)));
  const max = Math.max(0, ...totals.filter(Number.isFinite));
  const y = yScale(f, max);
  const baseline = y(0);
  const bw = x.bandwidth();
  const bars: Bar[] = [];
  for (const b of buckets) {
    const x0 = x(b.start) ?? f.m.left;
    let acc = 0;
    series.forEach((sr, i) => {
      const v = Number.isFinite(b.values[sr.id]) ? b.values[sr.id] : 0;
      if (stacked) {
        bars.push({ x: x0, y: y(acc + v), w: bw, h: baseline - y(v), series: sr.id, value: v, start: b.start });
        acc += v;
      } else {
        const w = bw / series.length;
        bars.push({ x: x0 + i * w, y: y(v), w, h: baseline - y(v), series: sr.id, value: v, start: b.start });
      }
    });
  }
  const every = Math.max(1, Math.ceil(starts.length / 10));
  const xTicks = starts.filter((_, i) => i % every === 0).map((s) => ({ x: (x(s) ?? 0) + bw / 2, label: formatDate(s, lang, g) }));
  return { bars, xTicks, yTicks: yTicksFor(f, max), baseline, centers: starts.map((s) => (x(s) ?? 0) + bw / 2) };
}

export function linePath(pts: { x: number; y: number }[]): string {
  return pts.filter((p) => Number.isFinite(p.x) && Number.isFinite(p.y)).map((p, i) => `${i ? 'L' : 'M'}${+p.x.toFixed(2)} ${+p.y.toFixed(2)}`).join(' ');
}

export function lineLayout(f: Frame, series: (Series & { points: { date: string; value: number }[] })[], lang: Lang): LineLayout {
  const all = series.flatMap((s) => s.points);
  if (all.length === 0) return { paths: [], xTicks: [], yTicks: yTicksFor(f, 0) };
  const dates = all.map((p) => p.date).sort();
  const domain: [Date, Date] = [parseYmd(dates[0]), parseYmd(dates.at(-1)!)];
  if (domain[0].getTime() === domain[1].getTime()) domain[1] = new Date(domain[1].getTime() + 86_400_000);
  const x = timeScale(domain, [f.m.left, f.m.left + f.innerW]);
  const max = Math.max(0, ...all.map((p) => p.value).filter(Number.isFinite));
  const y = yScale(f, max);
  const paths = series.map((s) => {
    const points = [...s.points].sort((a, b) => (a.date < b.date ? -1 : 1)).map((p) => ({ x: x(parseYmd(p.date)), y: y(p.value), value: p.value, date: p.date }));
    return { series: s.id, d: linePath(points), points };
  });
  const xTicks = dateTicks(domain, f.innerW, lang).map((t) => ({ x: f.m.left + t.x, label: t.label }));
  return { paths, xTicks, yTicks: yTicksFor(f, max) };
}

export function nearestIndex(xs: number[], x: number): number {
  let best = -1, dist = Number.POSITIVE_INFINITY;
  xs.forEach((v, i) => { const d = Math.abs(v - x); if (d < dist) { dist = d; best = i; } });
  return best;
}
```

- [ ] **Step 3: Komponente temelja**

`Chart.svelte`:

```svelte
<script lang="ts">
  // ZAŠTO OVAKO (cigla M2/54 — okvir grafa): jedan `viewBox` + `Frame` koji djeca dobivaju kroz
  // snippet; tooltip je HTML iznad SVG-a, položen u POSTOCIMA (x/width), pa ne treba mjeriti DOM.
  // `role="img"`/`aria-hidden` kao dosad (`svgA11y`).
  import type { Snippet } from 'svelte';
  import { frame, type Frame } from './layout';
  import Tooltip from './Tooltip.svelte';

  type Tip = { x: number; y: number; lines: string[] } | null;
  let { width = 600, height = 200, label, tip = null, children }: { width?: number; height?: number; label?: string; tip?: Tip; children: Snippet<[Frame]> } = $props();
  const f = $derived(frame(width, height));
  const a11y = $derived(label && label.trim() !== '' ? { role: 'img' as const, 'aria-label': label } : { 'aria-hidden': 'true' as const });
</script>

<div class="relative w-full">
  <svg viewBox="0 0 {width} {height}" {...a11y} class="w-full">
    {@render children(f)}
  </svg>
  {#if tip}
    <Tooltip {width} {height} {tip} />
  {/if}
</div>
```

`Axis.svelte`: `<line>` dna (`f.m.top + f.innerH`) i lijeve strane, `<text>` po `xTicks` (`text-anchor="middle"`, `y = f.height - 8`, `font-size="10"`, `fill="var(--color-ink-2)"`) i `yTicks` (`text-anchor="end"`, `x = f.m.left - 6`). `Grid.svelte`: `<line>` po `yTicks` preko `innerW`, `stroke="var(--color-line)"`. `Legend.svelte`: `<ul class="flex flex-wrap gap-3 text-xs text-ink-1">` s `<span class="inline-block h-2.5 w-2.5 rounded-sm" style="background: {color}" aria-hidden="true">` + natpis. `Tooltip.svelte`:

```svelte
<script lang="ts">
  let { width, height, tip }: { width: number; height: number; tip: { x: number; y: number; lines: string[] } } = $props();
  const left = $derived(Math.min(85, Math.max(0, (tip.x / width) * 100)));
  const top = $derived(Math.max(0, (tip.y / height) * 100));
</script>

<div class="chart-tip pointer-events-none absolute rounded-md border border-line bg-surface-1 px-2 py-1 text-xs text-ink-0 shadow-e2" style="left: {left}%; top: {top}%; transform: translate(-50%, -110%)" role="status">
  {#each tip.lines as line, i (i)}<div>{line}</div>{/each}
</div>
```

`motion.css`: `.chart-tip { animation: chart-fade var(--motion-dur) ease-out both; }` (koristi postojeći `@keyframes chart-fade`).

- [ ] **Step 4: Brane + commit**

`npm run check` · `npm run build`.

```bash
git add src/lib/charts/layout.ts src/lib/charts/Chart.svelte src/lib/charts/Axis.svelte src/lib/charts/Grid.svelte src/lib/charts/Legend.svelte src/lib/charts/Tooltip.svelte src/styles/motion.css tests/layout.test.ts
git commit -m "M2/54: temelji grafova -- layout.ts (okvir, stupci grupirani/naslagani, linija s datumima, najbliza tocka) + Chart/Axis/Grid/Legend/Tooltip; koordinate testirane, komponente tanke (S-035)"
```

---

### Task 55: `Bars` · `Line` · `Ring` · `Sparkline` na temeljima; `scale.ts` → `scales.ts`; Tempo prilagođen

**Files:**
- Modify: `apps/desktop/src/lib/charts/Bars.svelte`, `Line.svelte`, `Ring.svelte`, `Sparkline.svelte`
- Modify: `apps/desktop/src/lib/charts/scales.ts` (+ `arcPath`, `ringSegments`, `svgA11y`, `finiteMax` preseljeni iz `scale.ts`)
- Delete: `apps/desktop/src/lib/charts/scale.ts`, `apps/desktop/tests/scale.test.ts` (njegovi testovi sele u `tests/scales.test.ts`)
- Modify (iznimka vlasništva, samo pozivi grafova): `apps/desktop/src/views/Tempo.svelte`, `apps/desktop/src/views/Indicators.svelte` (ako se props Sparklinea ne mijenja — ne dira se)
- Test: `apps/desktop/tests/scales.test.ts`

**Interfaces:**
```ts
// Bars.svelte props
{ buckets: { start: string; values: Record<string, number> }[]; series: Series[]; granularity: Granularity; stacked?: boolean; height?: number; label?: string; valueText?: (v: number, series: string) => string }
// Line.svelte props
{ series: (Series & { points: { date: string; value: number }[] })[]; height?: number; label?: string; valueText?: (v: number, series: string) => string }
// Ring.svelte, Sparkline.svelte — props NEPROMIJENJENI (uvoz iz scales.ts umjesto scale.ts)
```

- [ ] **Step 1: Testovi presele** — `tests/scale.test.ts` → dopiši u `tests/scales.test.ts` (`arcPath`, `ringSegments`, `svgA11y`, `finiteMax`, `linePath` iz `layout.ts`); obriši `scale.test.ts`. Run: `npm run test` → FAIL (uvozi).

- [ ] **Step 2: Preseli pomoćnike** u `scales.ts` (doslovno, s komentarima), obriši `scale.ts`. `cumulative` se briše (nitko ga ne koristi — provjeri `grep -rn "cumulative(" src`).

- [ ] **Step 3: `Bars.svelte`**

```svelte
<script lang="ts">
  // ZAŠTO OVAKO (cigla M2/55 — stupci na temeljima): sve koordinate iz `barsLayout` (testirano);
  // ovdje samo `<rect>` po stupcu, tooltip na najbližem razdoblju (pokazivač ILI fokus tipkovnicom
  // — `tabindex="0"` na svakom stupcu), legenda kad ima više nizova.
  import Chart from './Chart.svelte';
  import Axis from './Axis.svelte';
  import Grid from './Grid.svelte';
  import Legend from './Legend.svelte';
  import { barsLayout, nearestIndex, type Series } from './layout';
  import type { Granularity } from './bucket';
  import { getLang } from '../i18n/index.svelte';
  import { formatDate } from './scales';

  type Props = { buckets: { start: string; values: Record<string, number> }[]; series: Series[]; granularity: Granularity; stacked?: boolean; height?: number; label?: string; valueText?: (v: number, series: string) => string };
  let { buckets, series, granularity, stacked = false, height = 200, label, valueText = (v) => String(v) }: Props = $props();
  const W = 600;
  let hover = $state<number>(-1);

  function tipFor(f: ReturnType<typeof barsLayout>, i: number) {
    if (i < 0 || i >= buckets.length) return null;
    const b = buckets[i];
    const lines = [formatDate(b.start, getLang(), granularity), ...series.map((s) => `${s.label}: ${valueText(b.values[s.id] ?? 0, s.id)}`)];
    return { x: f.centers[i], y: Math.min(...f.bars.filter((r) => r.start === b.start).map((r) => r.y)), lines };
  }
  function onMove(e: PointerEvent, centers: number[]) {
    const svg = e.currentTarget as SVGSVGElement;
    const r = svg.getBoundingClientRect();
    hover = nearestIndex(centers, ((e.clientX - r.left) / r.width) * W);
  }
</script>

{#if series.length > 1}<Legend items={series} />{/if}
<Chart width={W} {height} {label} tip={null}>
  {#snippet children(f)}
    {@const l = barsLayout(f, buckets, series, granularity, getLang(), stacked)}
    <Grid {f} yTicks={l.yTicks} />
    <Axis {f} xTicks={l.xTicks} yTicks={l.yTicks} />
    <g onpointermove={(e) => onMove(e, l.centers)} onpointerleave={() => (hover = -1)} role="presentation">
      {#each l.bars as b, i (i)}
        <rect class="chart-bar" x={b.x} y={b.y} width={Math.max(1, b.w)} height={b.h} rx="2"
          fill={series.find((s) => s.id === b.series)?.color}
          tabindex="0" onfocus={() => (hover = buckets.findIndex((k) => k.start === b.start))} onblur={() => (hover = -1)}>
          <title>{formatDate(b.start, getLang(), granularity)}: {valueText(b.value, b.series)}</title>
        </rect>
      {/each}
    </g>
  {/snippet}
</Chart>
```

Tooltip: `Chart` prima `tip` — u ovoj komponenti raspored se računa unutar snippeta, pa se `tip` računa iz `hover` u `$derived` s vlastitim `barsLayout(frame(W, height), …)` pozivom (isti ulaz, isti izlaz — čista funkcija) i predaje `Chart`-u: `const layout = $derived(barsLayout(frame(W, height), buckets, series, granularity, getLang(), stacked)); const tip = $derived(tipFor(layout, hover));` — i snippet koristi `layout` umjesto `@const`. (Graditelj bira ovaj oblik; `@const` gore je ilustracija.)

`Line.svelte`: isti obrazac s `lineLayout`; `<path class="chart-line" pathLength="1">` po nizu, `<circle class="chart-dot">` po točki, tooltip na najbližoj točki prvog niza po x. `Ring.svelte`/`Sparkline.svelte`: samo uvoz iz `./scales` (i `linePath` iz `./layout`).

- [ ] **Step 4: Tempo prilagođen (minimalno)**

`Tempo.svelte`: `import { bucketDays } from '../lib/charts/bucket';` · `const buckets = $derived(bucketDays(days, 'day').map((b) => ({ start: b.start, values: { [mode]: mode === 'commits' ? b.commits : b.hours } })));` · `<Bars buckets={buckets} series={[{ id: mode, label: t(`tempo.${mode}`), color: 'var(--color-brand-500)' }]} granularity="day" label={t(`tempo.${mode}`)} />` · `<Line series={[{ id: 'cum', label: t('tempo.cumulative'), color: 'var(--color-brand-500)', points: days.map((d) => ({ date: d.date, value: d.commits_cumulative })) }]} label={t('tempo.cumulative')} />`. (Prekidač dan/tjedan/mjesec dolazi u PLOČI T59.)

- [ ] **Step 5: Brane + commit**

`npm run check` · `npm run build`.

```bash
git add src/lib/charts tests/scales.test.ts src/views/Tempo.svelte
git rm -q src/lib/charts/scale.ts tests/scale.test.ts
git commit -m "M2/55: Bars/Line na temeljima (datumi na X-osi, mreza, legenda, tooltip), Ring/Sparkline na scales.ts, scale.ts uklonjen, Tempo prilagodjen -- postojeci grafovi dobivaju osi (S-035)"
```

---

### Task 56: `Heatmap` (kalendar) i `Histogram` (doba dana) + rasporedi

**Files:**
- Modify: `apps/desktop/src/lib/charts/layout.ts` (+ `heatmapLayout`, `histogramLayout`)
- Create: `apps/desktop/src/lib/charts/Heatmap.svelte`, `Histogram.svelte`
- Test: `apps/desktop/tests/layout.test.ts`

**Interfaces:**
```ts
export interface HeatCell { x: number; y: number; w: number; h: number; date: string; value: number; level: 0 | 1 | 2 | 3 | 4 }
export interface HeatmapLayout { cells: HeatCell[]; monthLabels: XTick[]; weekdayLabels: YTick[]; cell: number }
export function heatmapLayout(f: Frame, days: { date: string; value: number }[], range: [string, string], lang: Lang): HeatmapLayout;
// stupac = ISO tjedan, red = dan u tjednu (pon=0…ned=6); `level` = kvantil vrijednosti (0 = nema, 1–4), boje: level 0 = var(--color-surface-2), 1–4 = var(--color-brand-200/400/600/800) preko klasa
export interface HistogramLayout { bars: Bar[]; xTicks: XTick[]; yTicks: YTick[] }
export function histogramLayout(f: Frame, counts: number[]): HistogramLayout;   // 24 bina, x ticks 0·6·12·18 h
// Heatmap.svelte props: { days: { date: string; value: number }[]; range: [string, string]; label?: string; valueText?: (v: number) => string }
// Histogram.svelte props: { counts: number[]; label?: string }
```

- [ ] **Step 1: Padajući testovi** (`layout.test.ts`): `heatmapLayout` nad rasponom `2026-08-31`…`2026-09-27` (4 tjedna) → 28 ćelija, `x` ima 4 različite vrijednosti, `y` 7; dan s najvećom vrijednošću ima `level 4`, dan bez commita `level 0`; `monthLabels` sadrži `'ruj'`; prazan `days` → sve `level 0`, bez NaN. `histogramLayout(f, Array(24).fill(0))` → 24 stupca visine 0, `xTicks` natpisi `['0 h','6 h','12 h','18 h']`; `histogramLayout` s `counts[9] = 5` → stupac 9 najviši.

- [ ] **Step 2: Rasporedi** — `heatmapLayout`: `weeks = broj ISO tjedana u rasponu` (`bucketStart(range[0],'week')` do `bucketStart(range[1],'week')`), `cell = min(innerW / weeks, innerH / 7)`, ćelija `(col = indeks tjedna, row = (getUTCDay()+6)%7)`; `level` = 0 za 0, inače `1 + floor(3 * (v / max))` ograničeno na 4; `monthLabels` = prvi stupac svakog mjeseca s `formatDate(…, 'month')` skraćeno na mjesec (`%b`); `weekdayLabels` = `pon/sri/pet` (hr) / `Mon/Wed/Fri` (en) na redcima 0, 2, 4. `histogramLayout`: `scaleBand` nad `0..23`, y kao u `barsLayout`, ticksi na 0/6/12/18 s natpisom `${h} h`.

- [ ] **Step 3: Komponente** — `Heatmap.svelte`: `<rect class="chart-cell heat-{level}">` s `<title>{date}: {valueText(value)}</title>`, klase `.heat-0…4` u `motion.css`? NE — boje idu u `app.css` (nije pokret): `.heat-0 { fill: var(--color-surface-2) } .heat-1 { fill: var(--color-brand-200) } … .heat-4 { fill: var(--color-brand-800) }` (provjeri koje `brand-*` stepenice `tokens.css` ima; ako nema 200/800, uzmi postojeće četiri s rastućom tamnoćom i zapiši u izvještaj). `Histogram.svelte`: `Bars`-obrazac bez datuma.

- [ ] **Step 4: Brane + commit**

```bash
git add src/lib/charts/layout.ts src/lib/charts/Heatmap.svelte src/lib/charts/Histogram.svelte src/app.css tests/layout.test.ts
git commit -m "M2/56: Heatmap (kalendar po ISO tjednima, 5 razina) i Histogram (24 bina, doba dana) s rasporedima -- ritam i praznine vidljivi na jedan pogled (spec 3.2)"
```

---

### Task 57: `Gantt` (faze) i `HBars` (grane) + rasporedi

**Files:**
- Modify: `apps/desktop/src/lib/charts/layout.ts` (+ `ganttLayout`, `hbarsLayout`)
- Create: `apps/desktop/src/lib/charts/Gantt.svelte`, `HBars.svelte`
- Test: `apps/desktop/tests/layout.test.ts`

**Interfaces:**
```ts
export interface GanttRow { y: number; h: number; x0: number; x1: number; label: string; state: 'planned' | 'running' | 'closed'; from: string | null; to: string | null }
export interface GanttLayout { rows: GanttRow[]; xTicks: XTick[]; todayX: number | null; rowH: number }
export function ganttLayout(f: Frame, phases: Phase[], today: string, lang: Lang): GanttLayout;
// faza bez `from` → traka se ne crta (samo natpis); bez `to` a `running` → do `today`; `planned` bez datuma → natpis „(planirana)"; visina okvira = rows × rowH — komponenta računa height = m.top + m.bottom + phases.length × 22
export interface HBar { y: number; h: number; x: number; w: number; label: string; value: number; merged: boolean }
export interface HBarsLayout { bars: HBar[]; xTicks: XTick[]; rowH: number }
export function hbarsLayout(f: Frame, items: { label: string; value: number; merged: boolean }[]): HBarsLayout;   // vodoravno, silazno po vrijednosti kako stigne (ne sortira — `Report.branches` je već sortiran)
// Gantt.svelte props: { phases: Phase[]; today: string; label?: string }
// HBars.svelte props: { items: { label: string; value: number; merged: boolean }[]; label?: string; valueText?: (v: number) => string }
```

- [ ] **Step 1: Padajući testovi** — `ganttLayout` s tri faze (closed s from/to, running s from bez to, planned bez datuma) i `today='2026-09-23'`: 3 reda, closed `x1 > x0`, running `x1 === todayX`, planned bez trake (`x0 === x1`), `xTicks` s datumima; prazno → `rows: []`, `todayX: null`. `hbarsLayout` s tri stavke → širine proporcionalne, `bars[0].merged === true`, nula → `w === 0`.

- [ ] **Step 2: Rasporedi** — `ganttLayout`: domena = `[min(from), max(to ?? today)]` (+1 dan ako je prazna), `timeScale`, `rowH = innerH / max(1, rows)`, traka `h = rowH * 0.6`, `xTicks` iz `dateTicks`, `todayX = x(parseYmd(today))` ako je unutar domene. `hbarsLayout`: `x` linearna `[0, niceMax(max)]` → `[m.left, m.left + innerW]`, `rowH = innerH / max(1, n)`, `xTicks` iz `yTicks(max)`.

- [ ] **Step 3: Komponente** — `Gantt.svelte`: `<Chart>` s `height = m.top + m.bottom + phases.length * 22`, `<rect>` po redu s bojom po stanju (`closed` → `var(--color-ok)`, `running` → `var(--color-brand-500)`, `planned` → `var(--color-ink-3)` — imena tokena provjeri u `tokens.css`), natpis lijevo (`m.left` = 140 za nazive faza), `todayX` kao isprekidana `<line>`; `HBars.svelte`: natpis lijevo (`m.left` = 140), `<rect class="chart-bar">`, spojene grane s `opacity 0.6` + `<title>` „spojena".

- [ ] **Step 4: Brane + commit**

```bash
git add src/lib/charts/layout.ts src/lib/charts/Gantt.svelte src/lib/charts/HBars.svelte tests/layout.test.ts
git commit -m "M2/57: Gantt (faze od-do, danas kao crta) i HBars (grane, spojene prigusene) s rasporedima -- zadnja dva grafa za plocu (spec 3.2)"
```

**Orkestrator nakon spajanja GRAFOVA:** čuvar A — `ARCHITECTURE.md` tablica sučelja (`lib/charts/`: temelji + 8 komponenata, `scales.ts`/`bucket.ts`/`layout.ts`), `RUST.md` §2 npm-odlomak (4 nove ovisnosti s razlogom, S-035) i §5 (`scaleBand`, `Snippet<[Frame]>`, `$props` s funkcijom kao prop), `TESTING.md` §1 (rasporedi umjesto renderiranja).

---
## Tok PLOČA — tok Pregled → Projekt i nadzorna ploča (spec §3.1, §3.2, S-034)

Tek kad su DESKTOP-2, JEZGRA-2, IO-2 i GRAFOVI u `main`-u. Sve u `apps/desktop`.

### Task 58: Tok — `View` bez šest pogleda, izbornik od pet stavki, traka bez birača, `Project.svelte` sa sekcijama iz postojećih pogleda

**Files:**
- Modify: `src/lib/types.ts` (`View`, `VIEWS`), `src/lib/state.svelte.ts` (`enterProject`, `leaveProject`), `src/lib/shell/Sidebar.svelte`, `src/lib/shell/Topbar.svelte`, `src/App.svelte`, `src/views/Overview.svelte` (klik → `enterProject`)
- Create: `src/views/Project.svelte`, `src/views/project/sections.ts`, `src/views/project/SummarySection.svelte`, `TempoSection.svelte`, `KindsSection.svelte`, `PhasesSection.svelte`, `DeliveriesSection.svelte`, `IndicatorsSection.svelte`, `DocsSection.svelte` (sadržaj DOSLOVNO iz `views/{Tempo,Kinds,Phases,Deliveries,Indicators,Docs}.svelte`, `<h1>` → `<h2 id="sec-…">`)
- Delete: `src/views/Tempo.svelte`, `Kinds.svelte`, `Indicators.svelte`, `Phases.svelte`, `Deliveries.svelte`, `Docs.svelte`
- Modify: `src/lib/i18n/{hr,en}.json` (`nav.project`, `top.back`, `section.summary`, `section.branches`, `summary.*`), `tests/helpers.test.ts` (sekcije), `tests/explain.test.ts` (nepromijenjen — prefiksi ostaju), `tests/i18n.test.ts` ako provjerava `nav.*` popis

**Interfaces:**
```ts
// types.ts
export type View = 'overview' | 'project' | 'diary' | 'visions' | 'settings';
export const VIEWS: readonly View[] = ['overview', 'project', 'diary', 'visions'];   // 'settings' ostaje izvan (R22)
// state.svelte.ts
export async function enterProject(id: number): Promise<void>;   // currentId = id; view = 'project'; loadReport()
export function leaveProject(): void;                             // view = 'overview' (currentId ostaje — brz povratak)
// views/project/sections.ts
export const SECTIONS = ['summary', 'tempo', 'branches', 'kinds', 'phases', 'deliveries', 'indicators', 'docs'] as const;
export type Section = (typeof SECTIONS)[number];
export function sectionId(s: Section): string;    // `sec-${s}`
export function sectionKey(s: Section): string;   // i18n ključ naslova: 'section.summary' | 'nav.tempo' | 'section.branches' | 'nav.kinds' | 'nav.phases' | 'nav.deliveries' | 'nav.indicators' | 'nav.docs'
```

- [ ] **Step 1: Padajući test (`tests/helpers.test.ts`, nov `describe`)**

```ts
import { SECTIONS, sectionId, sectionKey } from '../src/views/project/sections';
import hr from '../src/lib/i18n/hr.json';
import en from '../src/lib/i18n/en.json';

describe('ploča projekta — sekcije', () => {
  it('osam sekcija, svaka s naslovom u oba jezika i jedinstvenim sidrom', () => {
    expect(SECTIONS).toHaveLength(8);
    const ids = new Set(SECTIONS.map(sectionId));
    expect(ids.size).toBe(8);
    for (const s of SECTIONS) {
      const key = sectionKey(s);
      expect((hr as Record<string, string>)[key], key).toBeTruthy();
      expect((en as Record<string, string>)[key], key).toBeTruthy();
    }
  });
  it('VIEWS više nema šest prikaznih pogleda', () => {
    expect(VIEWS).toEqual(['overview', 'project', 'diary', 'visions']);
  });
});
```

(`import { VIEWS } from '../src/lib/types'`.) Run → FAIL.

- [ ] **Step 2: `types.ts`, `state.svelte.ts`, `sections.ts`**

`state.svelte.ts` (uz `selectProject`, koji ostaje za Dnevnik/Vizije):

```ts
// Ulaz u projekt s Pregleda (S-034): kartica → ploča. Jedini put u pogled `project`.
export async function enterProject(id: number): Promise<void> {
  app.currentId = id;
  app.view = 'project';
  await loadReport();
}
// „‹ Pregled" u gornjoj traci; `currentId` ostaje da Dnevnik/Vizije/ponovni ulaz ne traže nov izbor.
export function leaveProject(): void {
  app.view = 'overview';
}
```

`sections.ts`:

```ts
// ZAŠTO OVAKO (cigla M2/58 — popis sekcija na JEDNOM mjestu, S-010): skok-izbornik, sidra i naslovi
// čitaju isti niz; test veže naslove na oba rječnika.
export const SECTIONS = ['summary', 'tempo', 'branches', 'kinds', 'phases', 'deliveries', 'indicators', 'docs'] as const;
export type Section = (typeof SECTIONS)[number];
const KEYS: Record<Section, string> = {
  summary: 'section.summary', tempo: 'nav.tempo', branches: 'section.branches', kinds: 'nav.kinds',
  phases: 'nav.phases', deliveries: 'nav.deliveries', indicators: 'nav.indicators', docs: 'nav.docs',
};
export const sectionId = (s: Section): string => `sec-${s}`;
export const sectionKey = (s: Section): string => KEYS[s];
```

- [ ] **Step 3: Okvir**

`Sidebar.svelte`: `{#each VIEWS as view}` — `overview` uvijek; `project`/`diary`/`visions` `disabled={app.currentId === null}` (klasa `opacity-50 cursor-not-allowed` kad je onemogućeno; `aria-disabled`); `settings` kao dosad. `Topbar.svelte`: `<select>` se BRIŠE; umjesto njega, kad `app.view !== 'overview' && app.view !== 'settings' && app.currentId !== null`: `<button onclick={leaveProject}>‹ {t('top.back')}</button><span class="font-medium">{ime projekta iz app.projects}</span>`; `RangePicker` i Osvježi ostaju. `onProjectChange` se briše. `App.svelte`: lanac pogleda = `overview | project | diary | visions | settings`; uvozi šest pogleda zamijenjeni `Project`. `Overview.svelte`: `onclick={() => void enterProject(project.id)}`.

- [ ] **Step 4: `Project.svelte` i sekcije**

```svelte
<script lang="ts">
  // ZAŠTO OVAKO (cigla M2/58 — nadzorna ploča projekta, S-034): jedna duga stranica, osam sekcija
  // iz JEDNOG popisa (`sections.ts`); skok-izbornik je `position: sticky` unutar `<main>` (koji
  // scrolla), sidra su `id="sec-…"` na `<h2>`. Sadržaj sekcija je preseljen iz bivših pogleda bez
  // promjene mjerenja — samo `<h1>` → `<h2>` i uvoz iz `./project/`.
  import { app } from '../lib/state.svelte';
  import { t } from '../lib/i18n/index.svelte';
  import { SECTIONS, sectionId, sectionKey } from './project/sections';
  import SummarySection from './project/SummarySection.svelte';
  import TempoSection from './project/TempoSection.svelte';
  import BranchesSection from './project/BranchesSection.svelte';
  import KindsSection from './project/KindsSection.svelte';
  import PhasesSection from './project/PhasesSection.svelte';
  import DeliveriesSection from './project/DeliveriesSection.svelte';
  import IndicatorsSection from './project/IndicatorsSection.svelte';
  import DocsSection from './project/DocsSection.svelte';
</script>

<div class="flex flex-col gap-8">
  <nav class="sticky top-0 z-10 -mx-4 -mt-4 flex flex-wrap gap-1 border-b border-line bg-surface-0/95 px-4 py-2 text-sm" aria-label={t('nav.project')}>
    {#each SECTIONS as s (s)}
      <a href="#{sectionId(s)}" class="rounded-md px-2 py-1 text-ink-1 hover:bg-surface-2">{t(sectionKey(s))}</a>
    {/each}
  </nav>
  {#if !app.report}
    <p class="text-sm text-ink-2">{t('common.loading')}</p>
  {:else}
    <SummarySection />
    <TempoSection />
    <BranchesSection />
    <KindsSection />
    <PhasesSection />
    <DeliveriesSection />
    <IndicatorsSection />
    <DocsSection />
  {/if}
</div>
```

`SummarySection.svelte` (novo, T58): `<h2 id="sec-summary">`, šest kartica s brojkama iz `app.report`: `touched.commits`, zbroj `days[].hours` (prikaz preko `hours()`), `days.length`, `deliveries.length`, `docs?.score ?? '—'`, `signals` po težini (`signalSummary` iz helpers); svaka je `<Explainable>` s POSTOJEĆIM id-om (`ind.commits`, `ind.hours`, `ind.working_days`, `ind.deliveries`, `docs.score`, `signals.severity`). `BranchesSection.svelte` (T58 = samo naslov + tablica `branches` name/commits/hours/lines/merged; graf u T59). Ostalih šest = preseljeni pogledi.

- [ ] **Step 5: i18n** — `nav.project`: „Projekt"/„Project"; `top.back`: „Pregled"/„Overview"; `section.summary`: „Sažetak"/„Summary"; `section.branches`: „Grane"/„Branches"; `summary.hours`, `summary.days`, `summary.deliveries`, `summary.docs`, `summary.signals` (kratki natpisi kartica); `branches.name/commits/hours/lines/merged/yes/no`. Stari `nav.tempo` … ostaju (naslovi sekcija).

- [ ] **Step 6: Brane + commit**

`npm run check` (explain.test: svi prefiksi i dalje postoje) · `npm run build`.

```bash
git add src tests
git rm -q src/views/Tempo.svelte src/views/Kinds.svelte src/views/Indicators.svelte src/views/Phases.svelte src/views/Deliveries.svelte src/views/Docs.svelte
git commit -m "M2/58: Pregled -> klik na karticu -> ploca Projekt (8 sekcija, skok-izbornik), izbornik od 5 stavki, traka bez biraca s '< Pregled' -- sest prikaznih pogleda postaju sekcije (S-034)"
```

---

### Task 59: Sekcije 2–4 — prekidač dan/tjedan/mjesec, toplinska karta, doba dana, grane, vrste kroz vrijeme

**Files:**
- Modify: `src/views/project/TempoSection.svelte`, `BranchesSection.svelte`, `KindsSection.svelte`
- Modify: `src/lib/i18n/{hr,en}.json` (`tempo.granularity.day|week|month`, `tempo.heatmap`, `tempo.hoursOfDay`, `branches.chart`, `kinds.overTime`)
- Test: `tests/helpers.test.ts` (ako dodaješ čistu funkciju, npr. `kindSeries()` za boje/natpise nizova — testiraj je)

**Interfaces:** Consumes `Bars`, `Line`, `Heatmap`, `Histogram`, `HBars`, `bucketDays`, `bucketKinds`, `hourHistogram`, `kindColor`, `kindLabel`. Produces: nova `views/helpers.ts::kindSeries(dict): Series[]` (pet nizova s bojom iz `kindColor` i natpisom iz `kindLabel`).

- [ ] **Step 1: Test `kindSeries`** — pet stavki, id-evi = `WorkKind` redom `planning…debugging`, boje iz `kindColor`. Run → FAIL.

- [ ] **Step 2: TempoSection** — stanje `mode: 'commits' | 'hours'` (postoji) + `granularity = $state<Granularity>('day')` s tri `<button aria-pressed>` (`tempo.granularity.*`); `buckets = bucketDays(days, granularity)`; `<Bars … granularity={granularity}>` i `<Line>` kumulativ (ostaje po danu); **novo:** `<Explainable id="tempo.heatmap" block><Heatmap days={days.map(d => ({ date: d.date, value: mode === 'commits' ? d.commits : d.hours }))} range={[app.report.since, app.report.until ?? today]} label={t('tempo.heatmap')} /></Explainable>` (`today` = `new Date().toISOString().slice(0,10)`; ista vrijednost koju Rust vraća kad je `until` `null` — prikaz, ne mjerenje) i `<Explainable id="tempo.hours_of_day" block><Histogram counts={hourHistogram(app.report.commits)} label={t('tempo.hoursOfDay')} /></Explainable>`. Tablica dana ostaje po danu.

- [ ] **Step 3: BranchesSection** — `<Explainable id="branches.bars" block><HBars items={app.report.branches.map(b => ({ label: b.name, value: mode === 'commits' ? b.commits : b.hours, merged: b.merged }))} label={t('branches.chart')} /></Explainable>` s prekidačem commiti/sati; ispod tablica iz T58; kad je `app.report.scope === 'default'` ili `branches.length === 1`: `<p class="text-xs text-ink-2">{t('branches.onlyDefault')}</p>` (spec: „mjerena samo zadana grana").

- [ ] **Step 4: KindsSection** — uz prsten: `<Explainable id="kinds.over_time" block><Bars buckets={bucketKinds(app.report.commits, 'week').map(b => ({ start: b.start, values: b.counts }))} series={kindSeries(getDict())} granularity="week" stacked label={t('kinds.overTime')} /></Explainable>`.

- [ ] **Step 5: Brane + commit** — `npm run check` (explain.test će PASTI na novim id-evima dok T61 ne doda tekstove: zato ovaj korak dodaje **privremeno** id-eve u `EXPLAIN_IDS` **i** tekstove `what/how/read` — NE, jedan izvor: T59 dodaje id-eve u `ids.ts` I tekstove u oba rječnika za SVOJA četiri grafa (`tempo.heatmap`, `tempo.hours_of_day`, `branches.bars`, `kinds.over_time`); T61 pokriva preostala tri i provjeru). Tekstovi po S-027 obrascu (što · kako · čitanje), s izvorom u kodu (npr. `how`: „`bucket.ts::hourHistogram` iz `CommitRow.author_time` u zoni ovog računala; jezgra ne nosi sat autora").

```bash
git add src tests
git commit -m "M2/59: Tempo s prekidacem dan/tjedan/mjesec, toplinskom kartom i dobom dana; Grane (HBars); Vrste rada kroz vrijeme (naslagano po tjednu) + 4 objasnjenja -- sekcije 2-4 ploce (spec 3.2)"
```

---

### Task 60: Sekcije 5–8 — Gantt faza, isporuke po tjednu, trend docs-ocjene i signala

**Files:**
- Modify: `src/views/project/PhasesSection.svelte`, `DeliveriesSection.svelte`, `DocsSection.svelte`
- Modify: `src/lib/i18n/{hr,en}.json` (`phases.gantt`, `deliveries.weekly`, `docs.trend`, `docs.trend.score|warn|alert`)
- Modify: `src/lib/explain/ids.ts` (+ `phases.gantt`, `deliveries.weekly`, `docs.trend`) i tekstovi u oba rječnika

**Interfaces:** Consumes `Gantt`, `Bars`, `Line`, `bucketDeliveries`, `api.getTrend(id, 'docs_score' | 'signals_warn' | 'signals_alert', range)`.

- [ ] **Step 1: PhasesSection** — iznad tablica: `<Explainable id="phases.gantt" block><Gantt phases={app.report.phases} today={app.report.today ?? todayYmd()} label={t('phases.gantt')} /></Explainable>` (`Report` nema `today` — koristi `new Date().toISOString().slice(0, 10)`; napomena na kartici).
- [ ] **Step 2: DeliveriesSection** — iznad tablice: `<Bars buckets={bucketDeliveries(rows, 'week').map(b => ({ start: b.start, values: { count: b.count, deploys: b.deploys } }))} series={[{ id: 'count', label: t('deliveries.title'), color: 'var(--color-brand-500)' }, { id: 'deploys', label: t('deliveries.deploy'), color: 'var(--color-accent)' }]} granularity="week" label={t('deliveries.weekly')} />` u `<Explainable id="deliveries.weekly" block>`.
- [ ] **Step 3: DocsSection** — `$effect` (obrazac iz `Indicators.svelte`, s `requestToken`) dohvaća tri trenda `api.getTrend(app.currentId, m, app.range)` za `m` u `['docs_score', 'signals_warn', 'signals_alert']`; `<Explainable id="docs.trend" block><Line series={[{ id: 'docs_score', label: t('docs.trend.score'), color: 'var(--color-brand-500)', points }, { id: 'signals_warn', …, color: 'var(--color-warn)' }, { id: 'signals_alert', …, color: 'var(--color-danger)' }]} label={t('docs.trend')} /></Explainable>`; kad su sve tri prazne: `<p class="text-xs text-ink-2">{t('docs.trend.empty')}</p>` („snimke postoje tek od dana dodavanja projekta").
- [ ] **Step 4: Brane + commit** — `npm run check` · `npm run build`.

```bash
git add src
git commit -m "M2/60: Gantt faza, isporuke po tjednu (deploy oznacen), trend docs-ocjene i signala iz dnevnih snimki + 3 objasnjenja -- sekcije 5-8 ploce (spec 3.2)"
```

---

### Task 61: Objašnjenja i i18n — 7 novih id-eva potvrđeno, prefiks `branches.`, tekstovi provjereni prema kodu

**Files:**
- Modify: `src/lib/explain/ids.ts` (konačan popis: 37 + 7 = 44), `src/lib/i18n/{hr,en}.json`, `tests/explain.test.ts` (prefiks `'branches.'` u „svaki pogled ima barem jedno objašnjenje"; `expect(EXPLAIN_IDS).toHaveLength(44)`)

- [ ] **Step 1: Test** — dodaj `'branches.'` u popis prefiksa i tvrdnju o 44 id-eva. Run → FAIL ako T59/T60 nisu dodali sve.
- [ ] **Step 2: Tekstovi** — svaki od 7 (`tempo.heatmap`, `tempo.hours_of_day`, `branches.bars`, `kinds.over_time`, `phases.gantt`, `deliveries.weekly`, `docs.trend`) ima `what/how/read` u oba jezika, **provjeren prema kodu** (isti standard kao T41/T42): `how` imenuje funkciju (`bucket.ts::bucketDays`, `metrics/branches.rs::branch_stats` — „sati dana po udjelu commita grane, proxy", `commit_sources` — „oznaka je jedna od grana koje commit sadrže; nakon spajanja prelazi pod zadanu"), `read` kaže rub (toplinska karta: prazan dan = bez commita, ne nula sati; doba dana: zona OVOG računala; trend: od dana dodavanja projekta, oznaka „profil promijenjen" na dan nadogradnje 1.0.0).
- [ ] **Step 3: Brane + commit** — `npm run check` (i18n hr = en, 283 + novi ključevi) · `npm run build`.

```bash
git add src/lib/explain/ids.ts src/lib/i18n tests/explain.test.ts
git commit -m "M2/61: 44 objasnjiva id-a (7 novih grafova), tekstovi provjereni prema kodu jezgre i bucket.ts -- svaki graf ploce ima karticu (S-027)"
```

---

### Task 62: Dimni test ploče (orkestrator, `tauri dev` nad klonom Sokrat Studyja, CDP)

**Files:** ništa u kodu; zapis u ledger `progress.md` + `task-62-report.md`.

- [ ] Klon Sokrat Studyja u scratchpad (`git clone --local` + `git worktree add` dva stabla s granama) — **ne pravi repo**; `npm run tauri dev -- --config "{\"identifier\":\"dev.sokratis.app.dev\"}"`; Leonu reći da ne dira dev-prozor.
- [ ] Pregled → klik na karticu → `app.view === 'project'` (CDP `invoke` ili DOM `h2#sec-summary` vidljiv); skok-izbornik vodi na `#sec-docs`; izbornik: Projekt/Dnevnik/Vizije onemogućeni prije ulaza; „‹ Pregled" vraća.
- [ ] Svih 11 grafova prisutno (`svg[role=img]` ≥ 11 + 18 sparklinea), X-osi imaju `<text>` s datumima (`\d{2}\.\d{2}\.`), legenda u naslaganim vrstama, tooltip na hover (CDP `Input.dispatchMouseEvent` → `.chart-tip` u DOM-u).
- [ ] Sekcija Grane pokazuje ≥ 2 grane s klona; Dnevnik ima stupac/oznaku grane (Dnevnik prikazuje `CommitRow.branch` — dodaj u T58 kao stupac tablice Dnevnika: da, `Diary.svelte` dobiva stupac „grana", **dopuna T58**).
- [ ] Četiri teme × `data-motion="off"`: snimke (CDP `Page.captureScreenshot`) 1280×800 i 960×600; kontrast brana već zelena.
- [ ] Nalazi → krug popravaka u PLOČI (graditelj), pa spajanje; **instalater „1.0.0-pre.4"**.

**Orkestrator nakon spajanja PLOČE:** čuvar A — `ARCHITECTURE.md` tablica sučelja (pet pogleda + `views/project/`), `TESTING.md` §5 dimni test ploče, `CLAUDE.md` Stanje.

---
## Tok IZDANJE — verzija, završna recenzija, čuvar, instalater (spec §7, §8; M2 §13.8 preuzet)

Sve tek kad je PLOČA u `main`-u i dimni test T62 zelen.

### Task 35 (nastavak): verzija 1.0.0 i mjerenja — u stablu `sokratis.rel` (grana `feat/release`)

Stablo stoji od S5 s pet necommitanih datoteka (`Cargo.toml`, `Cargo.lock`, `apps/desktop/package.json`, `package-lock.json` — samo `version`; `.sokratis/profile.json` + `test_path_exclude`). Brif `task-35-brief.md` + `task-35-dopuna.md` u mapi ledgera i dalje vrijede (R32–R34).

- [ ] **Orkestrator prije slanja graditelja:** `git -C ../sokratis.rel stash` → `git -C ../sokratis.rel merge --ff-only main` (grana nema commita, pa je ovo pomak na vrh `main`-a) → `git -C ../sokratis.rel stash pop` (sudar moguć SAMO u retcima `version` — riješi ručno na `1.0.0`) → `git -C ../sokratis.rel diff --stat` = točno tih pet datoteka.
- [ ] **Graditelj:** RED/GREEN `version.test.ts` (čitanjem), `npm ci` ne mijenja lock, `cargo test --workspace`, `npm run check`, mjerenja CLI ×3 nad Sokrat Studyjem (čitanje) u `task-35-report.md` (uz brojke iz `task-52-report.md` za usporedbu), commit `M2/35: verzija 1.0.0 …`. Orkestrator mjeri „commit → report_updated" ×3 u `tauri dev` nad klonom (R33).

### Task 63: Završna recenzija (opus) + jedan krug popravaka

- [ ] **Orkestrator:** dopuni `final-review-brief-S5.md` → `final-review-brief-1-0.md`: raspon `8edf3df..main` (sve od početka SUČELJA) **plus** tokovi ovog plana; `deferred-all-S5.txt` osvježen Grepom po ledgeru (`minor (deferred)`, `parked`, `Ruling`); naglasci: S-002 granica (jezgra bez I/O-a i s kartom grana), keš netaknut, paritet, `git_command` jedino mjesto, X/quit, `--branches` rub (`origin/content/*`), unija dnevnika „najnoviji pobjeđuje", `hourOfDay` zona, `types.ts` zrcalo, 44 objašnjenja provjerena prema kodu, `perf.rs` ≤ 8.
- [ ] Recenzent (opus) presuđuje u dva prolaza; nalazi → JEDAN krug popravaka (graditelj, sonnet) u `sokratis.rel` na vrhu `feat/release`; re-recenzija; spajanje `--no-ff` u `main`; pune brane; push.

### Task 43 (čuvar izdanja, S-031) — dopunjen

Brif `task-43-brief.md` vrijedi; **dopuna:** `ARCHITECTURE.md` prolazi tvrdnju po tvrdnju i za nove dijelove (§1 `io` sa `Scope`/`Lead`, desktop bez traya, §2 tok podataka s kartom grana i unijom dnevnika, §3 `Report` 17 polja, tablica profila `branch_scope`, sučelje: 5 pogleda + `views/project/` + `lib/charts/` temelji, CLI `--scope`) · `PROGRESS.md` drži samo M2 (M1 u `archive/`) · **ovaj plan** i `2026-09-18-m2-desktop.md` u `docs/archive/` s pečatom „✅ IZVRŠEN"; **spec 1.0.0** u `archive/` s pečatom „✅ ISPUNJEN <datum>"; `plan/` nosi samo `ROADMAP.md` · `CLAUDE.md` „Stanje" u desetak redaka (1.0.0 izgrađen; sljedeće M3 iz `BACKLOG.md`) · `README.md` (EN) opisuje 1.0.0: sve grane, ploča, X · `docs/README.md` indeks · `sokratis docs .` 100/100 · `signals .` nema.

### Instalater 1.0.0 → STANI

- [ ] `npm run tauri build` na `main`-u; sha256 i veličina u ledger; Leon instalira preko „1.0.0-pre.N". **Tag `v1.0.0`, izdanje na GitHubu i LICENCA čekaju Leonov izričit OK** (CLAUDE.md pravilo #1) — sesija STAJE ovdje.

---

## Samopregled plana (writing-plans, obavljen 2026-09-24)

1. **Pokrivenost speca:** §0 (šest nalaza) — T44 (4) · T45 (3) · T46–T51 (1) · T53–T57 + T59–T60 (2) · T58 (5, 6) ✓ · §1.1 `Scope`/karta/keš/prozor/`ref_name` — T49 ✓ · §1.2 model/`branch_stats`/sati po udjelu/`Report.branch`/pravila/paritet/snapshot — T46 ✓ · §1.3 profil/CLI/`origin/content/*` prihvaćeno/trend-oznaka — T48, T51, napomena T48 ✓ · §2 unija/vodeće stablo/`Touched`/watcher nepromijenjen/+2 procesa — T47, T50, T52 ✓ · §3.1 tok/traka/izbornik — T58 ✓ · §3.2 osam sekcija, 7 novih grafova, `explain`, doba dana u zoni računala, trend od dodavanja — T58–T61 ✓ · §3.3 temelji + 8 komponenata + `layout.ts` — T53–T57 ✓ · §3.4 dijalog — T45 ✓ · §4.1/§4.2 — T45/T44 ✓ · §5 tablica testova — svaki red ima ciglu (paritet: nepromijenjen; `perf.rs`: T49/T50/T52; dimni: T45-orkestrator, T62) ✓ · §6 ovisnosti — T53 (4 npm), T45 (feature van) ✓ · §7 redoslijed — tablica sesija ✓ · §8 izlazni uvjet — T52 (1, 2), T45+dimni (3), T62 (4, 5), T43 (6) ✓ · §9 izvan opsega — ništa u planu ✓.
2. **Placeholderi:** nema „TBD/TODO"; mjesta gdje graditelj bira konkretan broj su označena („upiši STVARNE verzije", „IZRAČUNA i upiše broj") — to su mjerenja, ne rupe. Rasporedi T56/T57 su opisani pravilima (domena, `rowH`, `level`), s testovima koji ih fiksiraju.
3. **Tipovi:** `Scope<'_>` u `log`/`rev_list`/`cached_log` (T49) ↔ pozivi u `project.rs` (T49/T50) ✓; `commit_rows(…, commit_branches, default_branch)` (T46) ↔ `report.rs` ✓; `last_changes(rev, pathspecs)` (T50) ↔ `docs_in` i `tests/last_changes.rs` ✓; `Bars` props (T55) ↔ `TempoSection`/`KindsSection`/`DeliveriesSection` (T59/T60) ✓; `Series` iz `layout.ts` ↔ `kindSeries` (T59) ✓; `View`/`VIEWS` (T58) ↔ `Sidebar`/`App` ✓; `Api.quit/onCloseRequested` (T45) ↔ `App.svelte` ✓; `BranchScope` serde `all`/`default` (T46) ↔ profil (T48), CLI (T51), TS `'all' | 'default'` (T46) ✓.
4. **Review Focus:** #1 → T49/T50/T46 · #2 → T50 · #3 → T49 · #4 → T46/T53/T54 · #5 → T50 · #6 → T51 — svaki ima imenovani test ✓.
5. **Ispravci tijekom samopregleda:** unix-vremena u fixtureima T46/T50 izračunata (2026-09-10T08:00+02:00 = 1789020000; 2026-09-10T10:00+02:00 = 1789027200); Dnevnik dobiva stupac „grana" (dopuna T58, spec §3.2 red 1 „Sažetak" ne pokriva Dnevnik, ali S-032 traži da se grana vidi po commitu — dodano u T58 Step 4).
