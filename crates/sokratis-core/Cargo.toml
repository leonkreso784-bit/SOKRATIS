[package]
name = "sokratis-core"
version.workspace = true
edition.workspace = true
authors.workspace = true
publish.workspace = true

[dependencies]
serde.workspace = true
regex.workspace = true
thiserror.workspace = true

# `serde_json` jezgri ne treba za rad — koristi se samo u testovima (fixture `expected.json`,
# provjere serijalizacije). Pravilo #6: svaka ovisnost je namjerna i na svom mjestu.
[dev-dependencies]
serde_json.workspace = true
# `insta` se vraća (M1 ga je uklonio kao neiskorišten): snapshot `Report`-a je od M2 ugovor prema
# sučelju (S-022) — dev-ovisnost, jezgra ga u radu ne koristi.
insta.workspace = true
