use crate::Commit;
use std::collections::BTreeMap;
pub fn hours_per_day(commits: &[Commit], gap_h: f64, start_h: f64) -> BTreeMap<String, f64> {
    let _ = (commits, gap_h, start_h);
    todo!("cigla M1/8")
}
