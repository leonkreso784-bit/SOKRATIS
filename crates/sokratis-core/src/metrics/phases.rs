use crate::{Commit, Patterns, Phase};
pub fn closed_phases(all_commits: &[Commit], p: &Patterns) -> Vec<Phase> {
    let _ = (all_commits, p);
    todo!("cigla M1/11")
}
pub fn active_phases(plan_phases: Vec<Phase>, commits: &[Commit], today: &str) -> Vec<Phase> {
    let _ = (plan_phases, commits, today);
    todo!("cigla M1/11")
}
